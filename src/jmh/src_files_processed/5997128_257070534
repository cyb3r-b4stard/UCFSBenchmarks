import info.gridworld.grid.*;
import info.gridworld.actor.*;;
import java.util.*;
import java.io.*;
import static java.lang.System.out;
public class BugRunner
{
    public static void main( String[] args ) throws IOException
    {
        GameWorld world = new GameWorld(new BoundedGrid<Actor>(33,49));
        world.add(new BombCritter(world));
        world.add(new FlowerCritter(world));
        world.add(new Slime());
        world.add(new RedCritter(world));
        world.add(new Dragon(world));
        BoundedGrid<Actor> gr = new BoundedGrid<Actor>(33,49);
        gm.lvlChange();
        world.add(new Location(0,0),GameWorld.bug);
        world.add(new Location(32,48),GameWorld.exit);
        world.show();
    }
    /*public static void populate(BoundedGrid<Actor> bg)
    {
        clone = new BoundedGrid<Actor>(33,49);
        for(Location loc : bg.getOccupiedLocations())
        {
            clone.put(loc,bg.get(loc));
        }
    }
    public static BoundedGrid<Actor> grid(){return clone;}
    public static void main2(BoundedGrid<Actor> gr)
    {
        GameWorld world = new GameWorld(new BoundedGrid<Actor>(33,49));
        ArrayList<Actor> al = null;
        ArrayList<Location> locs = null;
        for(Location loc: gr.getOccupiedLocations())
        {
            al.add(gr.get(loc));
            locs.add(loc);
            gr.get(loc).removeSelfFromGrid();
        }
        for(int j = 0; j < al.size(); j++)
        {
            world.add(locs.get(j),al.get(j));
        }
        world.add(new BombCritter(world));
    }*/
}