
/**
 * ######### # "#### ####" #### ##### ## ### ########### ### #####.
 * 
 * @###### ####### ######## 
 * @####### ####.##.##
 */
public class StarWarsNameGenerator
{
    private String firstName;
    private String lastName;
    private String maidenName;
    private String city;
    private String car;
    private String medication;

    /**
     * Constructor for objects of class StarWarsNameGenerator
     * 
     * @param firstName the input of the users first name
     * @param lastName the imput of the users last name
     * @param maidenName the imput of the users mothers maiden name
     * @param city the imput of the users birth city
     * @param car the imput of the users first car
     * @param medication the imput of the users last ingested medication
     */
    public StarWarsNameGenerator(String firstName, String lastName, 
    String maidenName, String city, String car, String medication)
    {
        this.firstName = firstName;
        this.lastName = lastName;
        this.maidenName = maidenName;
        this.city = city;
        this.car = car;
        this.medication = medication;
    }

    /**
     * Creates the first and last name 
     * 
     * @return the first and last name   
     */
    public String getName()
    {
        return firstName.substring(0,1).toUpperCase() 
        + firstName.substring(1,3).toLowerCase
        + lastName.substring(0,2).toLowerCase() 
        + " " 
        + maidenName.substring(0,1).toUpperCase 
        + city.substring(0,3).toLowerCase();
        
        
    }
    
    /**
     * Creates the title of the users Star Wars charater
     * 
     */
    public String getHonorific()
    {
        return lastName.substring(lastName.lenght() - 1 + 
        lastName.substring(lastName.length() - 2, lastName.length() - 1) + car.
    }
}