class srh
{private String s="subhash chandra bose",s2="",s3="";
    int i=0,d=0;
    public void display()
    {
      s2=s.charAt(0)+".";
        for(i=0;i<s.length();i++)
        {if(s.charAt(i)==' ')
            {d=d+1;
               if(d==1)
              s3=s.charAt(i+1)+".";
               if(d==2)
               System.out.print(s.substring((i+1),s.length()+s2+s3
            }}}}