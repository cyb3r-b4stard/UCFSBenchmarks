
/* #### ### #### ### ########## # 
 * ####:
 * ########:
 * ##:
 */

import ecs100.*;

/** ####### ## ###### ###### ######## ####### ###### ##### ###
 *  ############# #####.  
 */

public class CartoonStrip{

    /** animate creates two cartoon figures on the window.
     *  Then animates them according to a fixed script by calling a series
     *  of methods on the figures.
     */
    public void animate(){
        /*# YOUR CODE HERE */
        UI.clearPanes();
        CartoonFigure jack = new CartoonFigure("guy2",50,100);
        jack.toast();
        jack.watchTV(30,20);
        CartoonFigure sally = new CartoonFigure("girl4",350,110);
        sally.watchTV(15,20);
        sally.boilWater(10);
        jack.bake(30);
        sally.washLoad(false,"cold");
        jack.getPowerBill();
        sally.getPowerBill();
        jack.payPowerBill();
        sally.payPowerBill();
        jack.go();
        sally.go();
    
    }

    /** For the completion, the story must be a gamebook story
     *      where the user is asked to make decisions about what the characters will do.
     *   At the end it must print out the users power bill for the story that they chose.
     */
    public void animateCompletion(){
        UI.clearPanes();
        /*# YOUR CODE HERE */
        UI.println("Jack just got home from work");
        CartoonFigure jack =new CartoonFigure("guy3",50,50);
        boolean toast = UI.askBoolean("Jack is feeling hungry, should he cook some toast before Sally gets home?");
        if(toast){
            jack.toast();
            UI.println("Jack makes some toast");
        }
            else {
                UI.println("Jack doesn't feel like toast");
                boolean jreheat = UI.askBoolean("Does he want to reheat some leftovers instead?");
                if(jreheat){
                    jack.reheatFood(5,1000);
                    UI.println("Jack reheats some leftovers");
                }
                    else{
                        UI.println("Jack decides he will wait for Sally to get home before eating");
                    }
                
            }
            boolean tv = UI.askBoolean("Jack checks the time and notices he has half an hour before Sally gets home, should he watch TV?");
            if(tv){
                jack.watchTV(30,20);
                UI.println("Jack watches some TV to kill some time");
            }
            else{
                UI.println("I suppose Jack will sit in silence or read a book by himself");
            }
            boolean door = false;
            UI.println("Jack thinks he hears a knock at the door");
            boolean door = askBoolean("Does Jack go and check the door?");
            if(door)
                    
    
    }

    /** ---------- The code below is already written for you ---------- **/
        /** Constructor: set up user interface */
    public CartoonStrip(){
        UI.initialise();
        UI.addButton("Clear", UI::clearGraphics );
        UI.addButton("Animate", this::animate );
        UI.addButton("Animate Completion", this::animateCompletion );
        UI.addButton("Quit", UI::quit );
        UI.setDivider(0);       
    }


}
