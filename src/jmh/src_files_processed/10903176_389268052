
/**
 * ##### ######### ##### ######### ##### ########## ### #### ###### ###### ### 
 * ################## ###### ########### ###-### 
 * @###### ###
 */
public class FatWombat {

    /**
     * integer value yang menyatakan lingkar pinggang wombat (HC)
     * lingkar pinggang dalam sentimeter, misal 100 sentimeter
     */
    private int hip;
    
    /**
     * double value yang menyatakan tinggi dari wombat (HM)
     * tinggi dalam meter, misal 1.47 meter
     */
    private double height;
    
    /**
     * integer value yang menyatakan umur wombat
     */
    private int age;
    
    /**
     * boolean value yang menyatakan apakah wombat ini wombat jantan atau betina
     * isMale berisi TRUE jika wombat jantan, FALSE jika wombat betina
     */
    private boolean isMale;
    
    /**
     * Constructor untuk kelas FatWombat
     * @param hip menyatakan lingkar pinggang dari wombat, asumsikan bahwa nilai
     * yang dimasukkan selalu antara 30 s.d. 200
     * @param height menyatakan tinggi dari wombat, asumsikan bahwa nilai yang 
     * dimasukkan selalu antara 1.22 s.d. 2.44)
     * @param age menyatakan umur wombat, asumsikan bahwa nilai yang dimasukkan 
     * selalu antara 20 s.d. 79
     * @param isMale true jika wombat jantan, false jika wombat betina
     */
    public FatWombat(int hip, double height, int age, boolean isMale) {
        this.hip = hip;
        this.height = height;
        this.age = age;
        this.isMale = isMale;
    }
    
    /**
     * mengklasifikasikan wombat ini berdasarkan nilai BAI-nya dan umur wombat.
     * Lihat rumus yang ada di lembar soal. Nilai BAI di rumus sudah dalam persen.
     * @return mengembalikan string berisi kata "Kekurangan Berat" atau 
     * "Normal" atau "Kelebihan Berat" atau "Obesitas" (tanpa tanda kutip 2)
     */
    public String getBodyFatClassification() {        
        /***======================================================***/
        /*** anda hanya perlu mengisi mulai dari baris setelah ini***/
        double BAI;
        BAI=(this.hip/(Math.pow(this.height,1.5)))-18;
        String result;
        /*** silahkan menambahkan method/atribut lain yang anda anggap perlu ***/
        /***=================================================================***/
        if(isMale==true){
            if(this.age>=20&&this.age<=30){
                if(
    }
    
    /**
     * method ini dapat digunakan untuk mengklasifikasikan wombat dengan cepat
     * sehingga wombat tidak perlu di-new() lagi untuk mencoba tes kasus yang
     * lain.
     * @param hip lihat keterangan di constructor
     * @param height lihat keterangan di constructor
     * @param age lihat keterangan di constructor
     * @param isMale lihat keterangan di constructor
     * @return lihat keterangan return value di method getBodyFatClassification()
     */
    public String getBodyFast(int hip, double height, int age, boolean isMale) {
        this.hip = hip;
        this.height = height;
        this.age = age;
        this.isMale = isMale;
        return this.getBodyFatClassification();
    }
    
}