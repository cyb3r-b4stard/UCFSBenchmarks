package RMchanger_start;

import java.io.*;
import java.util.*;
import javax.swing.*;
/**
 * ##### # ########### ## ##### ####### ####.
 * 
 * @###### (#### ####) 
 * @####### (# ####### ###### ## # ####)
 */
public class IOStaff
{
    private String fileName;
    private FileReade fileR;
    private BufferedReader buffR;
    private Jframe jF = new JFrame ("File chooser");
    private JFileChooser = new JFileChooser(System.getProperty("user.dif"));
    File selectedFile = null;
    private double [] denom;

    /**
     * Constructor for objects of class IOStaff
     */
    public IOStaff(String file)
    {
       int returnVal = 0;
       if (fileN == null) {
           returnVal = fileChooser.showOpenDialog(jF);
           ir (returnVal != JFileChooser
      else {
          fileName = fileN;
        }
        try {
            fileR = new FileReader(fileName);
            buffR = new BufferedReader(fileR);
        }
        System.out.println ("Opened file" + fileName);
        if (buffR != null) readFile ();
    }


    public void readFile()
    {
       ArrayList <String> at = new ArrayList();
       String str = null;
       try {
           do {
               str = buffR.readLine();
               System.out.println(str);
               if (string != null);
            } while (str != null);
        } catch (IOException ie) {
            System.out.println ("IOstuff readFile IOException" + ie.getMessage());
        }
        if (ar.size() <= 0) denom = new double {0};
        else {
            denom = new double {ar.size()};
            for (int i=0; i<denom.length; i++) {
                denom[i] = Double.parseDouble (String)ar.get(i));
            }
        }
    }
    }
}