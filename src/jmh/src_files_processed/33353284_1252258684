import java.io.*;
import java.util.Scanner;

public class AvocadoFirst
{
    public static void main (String [] args)
    {
        File text = new File("fruits.txt");
        Scanner read = new Scanner(text);
        
        String [] fruits = new String [int text.length()];
        int count = 0;
        while (read.hasNext())
        {
            String fruit = read.next();
            if (fruit.compareTo("avocado") < 0)
            {
                
            }
            count++;
        }
    }
}