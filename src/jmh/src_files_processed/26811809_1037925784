public class Mathe {
    public double bcos (double b){
        return (1-Math.pow(x,2)/2+Math.pow(x,4)/24-Math.pow(x,6)/720+Math.pow(x,8)/40320-Math.pow(x,10)/3628800);
    }
    public double cosinus (double g) {
        double b = 0;
        b=2*Math.PI< /360*g;
        return (bcos(b));
