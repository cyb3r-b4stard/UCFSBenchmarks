import java.util.Scanner;

public class Main
{
    String[] alphabet = new String[26];

    public Main()
    {
        buildAlphabet();

        System.out.print('\u000c');
        Scanner in = new Scanner(System.in);
        String choice = "";

        while(true)
        {
            System.out.print("[E]ncode, [D]ecode, [Q]uit? ");
            choice = in.nextLine();

            if(choice.equals("Q"))
                break;

            System.out.print("Enter phrase: ");
            String text = in.nextLine();    
            System.out.print("Enter number of shifts: ");
            int shifts = in.nextInt();  
            in.nextLine();      

            if(choice.equals("E"))
                System.out.println( "Encoded text: " + encode(text,shifts) );
            else if(choice.equals("D"))
                System.out.println( "Decoded text: " + decode(text,shifts) );
            System.out.println();
        }
    }

    private String encode(String plainText, int shifts)
    {
        String encoded = "";
        
        for(int x=0;x<text.length;x++){
        String b="";
        b=text.substring(x,x+1);
        
        for(int i=0;b.equals(alphabet[i]);i++){
            alphabet[i];
        }
        
        encoded=encoded+s;
        return encoded;
    }

    private String decode(String cipherText, int shifts)
    {
        String decoded = "";
        
        
        return decoded;
    }

    private void buildAlphabet()
    {
        for(int i=0;i<alphabet.length;i++)
            alphabet[i]=""+(char)(65+i);
        
    }
}