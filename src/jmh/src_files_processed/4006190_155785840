
public class KUNDE extends DE
{
    String nn;
    String vn;
    public KUNDE(String nnn, String nvn)
    {
       nn = nnn;
       vn = nvn;
    }

    int vergleichen(data){
       if(nn.compareTo(data.nn)>0){
          return 1;
       }else
       if(nn.compareTo(data.nn)<0){
          return -1;
       }else return 0;
    }
}