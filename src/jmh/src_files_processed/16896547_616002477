import java.io.*;
import java.lang.*;
class ReverseName
{
    private char name;
    private String s;
    public void main (String args[])throws IOException
    {
        InputStreamReader isr=new InputStreamReader(system.in);
        BufferedReader br=new BufferedReader(isr);
        System.out.print("Enter your name");
        s=br.readLine();
        name=char.parseChar(s);
        name=reverseName;
        System.out.print("The reverse of your name is"+name);
    }
}