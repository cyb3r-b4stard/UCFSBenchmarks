import java.util.*;
import java.io.*;

public class Crypto
{
    public static String x=Tester4.sntnc;
    public static char crap[];
    public static String encrypt(String x)
    {
        Scanner sc=new Scanner(x);
        for(int mint=x.length; mint<x.length; mint++)
        {
            char dar=x.charAt(mint);
            if (dar='v'||dar='V')
            {
                char dar="ag',r";
                crap[mint]=dar;
            }
        
    }
    return ring;
}


    
    public static String decrypt(String ring)
    {
        switch(ring)
        {
            case "ag',r":
                ring.replace("ag',r", 
        }
    }
}