
/**
 * ##### #####  
 * ########
 * ###### #-###
 * 
 */
import java.util.Scanner;

class getVals
{
    double[] yarra;
    double highLimit = 10;
    double lowLimit = 1;

    public boolean testValue(double myVal)
    {
        if ( lowLimit <= 1.0 && highLimit >= 10.0)
            return true;
        else
            return false;

    }

    public double getAdouble()
    {
        Scanner scan = new Scanner(System.in);
        double aDouble;
        aDouble = scan.nextDouble();
        return aDouble;
    }

    public int getAPositiveint ()
    {
        Scanner scan2 = new Scanner(System.in);
        int positiveint;
        positiveint = scan2.nextInt ();
        if  (positiveint >=0)

            return positiveint;
        else { 
            positiveint = positiveint *-1;
        }

        return positiveint;

    }
    public void fillArray (int numElems)
    {
        
    }
    
    public double getAverage () 
    {
        double runningTotal = 0;
        for( int i = 0 ; i<yarra.length; i ++)
        {
           runningTotal += yarra [i];
        }
        double average = runningTotal / yarra.length;
        return average;
    }
    
    public double getMax() 
    {
        double max = 0;
        for ( i = 0; i < yarra.length; i++)
        {
            if (yarra [i] > max)
            max - yarra[I];
            return max
        }
    }
}
