import java.util.Scanner;

public class Game
{
    int x = player.getX();
    
   
    public Game()
    {
        Player player = new Player(int player.getX(), int player.getY());
        
        Dot dot1 = new Dot(dot1.getX(), dot1.getY());
        Dot dot2 = new Dot(dot2.getX(), dot2.getY());
        Dot dot3 = new Dot(dot3.getX(), dot2.getY());
    }

    
}