/**
 * #### ### ##### ################ ### ######### ########
 * 
 * @###### ###### ##########
 * @####### #.## ##.##.##
 */
public class Aufgabensammlung
{
    /** 
     * Aufgabe 1
     * Diese Methode verlgeicht die Zahl 1 und Zahl 2, welche das grösser ist.
     * Falls die Werte gleich gross sind, wir ein Meldung ausgegeben
     */
    public void welcheZahlistGroesser(int zahl1, int zahl2)
    {
        if (zahl1 > zahl2) {
            System.out.println("Die grösste Zahl ist " +zahl1);           
        }

        if (zahl1 < zahl2) {
            System.out.println("Die grösste Zahl ist " +zahl2); 
        }

        if (zahl1 == zahl2) {
            System.out.println("Diese Zahlen sind gleich gross");
        }
    }

    /** 
     * Aufgabe 2
     * Diese Methode vergleicht die Zahl 1, Zahl 2 & Zahl 3, anschliessend wir die grösste Bestimmt
     */
    public int bestimmediegroessteZahl(int zahl1, int zahl2, int zahl3)
    { 
        int groessteZahl;
        groessteZahl = zahl1;
        if (groessteZahl<zahl2) {
            groessteZahl = zahl2;
        }
        else if (groessteZahl<zahl3) {
            groessteZahl = zahl3;
        }
        return groessteZahl;        
    }

    /** 
     * Aufgabe 3
     * Diese Methode prüft ob es sich um eine gerade, ungerade, positive oder negativ Zahl handelt.
     * Es können mehrer Fälle vorhanden sein
     */
    public void pruefeZahl (int zahl)
    { 
        if (zahl%2==0) {
            if (zahl>0) {
                System.out.println("Dies ist eine gerade und positive Zahl");  
            }
            else {
                System.out.println("Dies ist eine gerade und negative Zahl");
            }
        }
        else {
            if (zahl>0) {
                System.out.println("Dies ist eine ungerade und positive Zahl");
            }
            else {
                System.out.println("Dies ist eine ungerade und negative Zahl"); 
            }
        }
    }}
    
    /** 
     * Aufgabe 4
     * Diese Methode teil die grössere Zahl durch die Kleiner
     * Es darf jedoch nicht durch 0 geteilt werden
     */
    public int teileZahl (int zahl1, int zahl2)
    {
        int ergebnis
        if (zahl1>zahl2) {
            if (zahl2>0) {
                ergebnis=zahl1/zahl2;
                return ergebnis;
            }
            else {
                System.print.outln("Es darf nicht durch 0 dividiert werden!");
            }                
        }
        else {
            if (zahl1>0){
                ergebnis=zahl2/zahl1;
                return ergebnis;
            }
            else {
                System.print.outln("Es darf nicht durch 0 dividiert werden!");
            }}
        }
        
                            
        