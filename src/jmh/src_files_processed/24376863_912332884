import java.util.Scanner;
import java.io.file;

public class USMNames
{ 
    public static void main(String[] args) throws exception;
     File namesFile = new File("USMNames.txt");
        Scanner fin = new Scanner(namesFile);
    
        while (fin.hasNext())
    {
        String line = fin.nextLine();
        System.out.println(line);
}
}