import uulib.*;
public class Volume 
{
    public static double volumeSphere(double d)
    
        double n;
        double p;
        double pi = Math.PI;
        double volSphere = (pi*(Math.pow(n, p)))/6;
        
        return
        
}