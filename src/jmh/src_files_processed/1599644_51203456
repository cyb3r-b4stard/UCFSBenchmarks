import java.util.*;
import java.lang.Object;

public class EfficientTester {
    public static void main (String[] args) {
        ArrayList <BankAccount> accntdb = new ArrayList <BankAccount>();
        ArrayList <String> activityLog = new ArrayList <String>();

        Scanner intinp = new Scanner(System.in);
        Scanner stringinp = new Scanner(System.in);

        System.out.println("\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nWelcome to the BBA (Bank of the Better Abi)");

        int bnkaccnt_loop = 1;
        while(bnkaccnt_loop > 0) {
            System.out.println("\nHere are your options:"
                + "\n       1. Create a New Account"
                + "\n       2. Access an Existing Account"
                + "\n       3. Quit this Program");       
            int accountchoice = intinp.nextInt(); 

            if(accountchoice.hasNext(int) == false) {
                System.out.println("\n**NOTICE:** \n    Please enter a number to choose an option.");
            }
            if(accountchoice.hasNextInt() == true) {

                if(accountchoice == 1) {
                    System.out.println("\nPlease enter your legal FIRST and LAST name:");
                    String usrname = stringinp.nextLine().toUpperCase(); 

                    System.out.println("\nWould you like to create a:"
                        + "\n       1. Savings Account"
                        + "\n       2. Checking Account"
                        + "\n       3. Go Back to the Main Menu");
                    int bnksetup = intinp.nextInt();

                            
                }
            }
        }
    }
}