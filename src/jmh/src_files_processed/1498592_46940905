

import java.util.Scanner;

public class Temps
{
    public static void main (String[] args)
    {
    final int HOURS_PER_DAY = 24;
    int hour = 1;
    int max = -1000;
    int min = 1000;

    int temp;   

    Scanner scan = new Scanner(System.in);
    int

   
System;
    
    do
        max = temp;
        while (temp < max)
    {do
        min = temp;
        while (temp > min);}

    for (int hour = 0; hour < HOURS_PER_DAY; hour++)
    {
        System.out.print ("Enter the temperature reading at " + hour +
                " hours: ");
        temp = scan.nextInt();
    }

    System.out.println("The maximum temperature was: " + max + "The minimum temperature was: " + min);
    
}
