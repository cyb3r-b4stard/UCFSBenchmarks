import java.io.*;
class encoding
{
    public static void main() throws IOException
    {
        DataInputStream in=new DataInputStream (System.in);
        System.out.println("Enter the nnmber of sentence ");
        int n=Integer.parseInt(in.readLine());
        if(n>=1 && n<10)
    }
}
            
 