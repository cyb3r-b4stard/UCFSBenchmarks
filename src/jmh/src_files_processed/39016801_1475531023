import java.io.*;
class Angles
{
    public static void main(String arcs[])throws IOException
    {
        InputStreamReader read=new InputStreamReader(System.in);
        BufferedReader in=new  BufferedReader(read);
        int a,b;
        System.out.println("Enter the Two angles");
        if(a+b==90)
        {
        System.out.println("The No. is a commplementary angle");
        }
        else(a+b==180)
        {
            System.out.println("The no. is a Supplementary angle");
        }
    }
}
        
        