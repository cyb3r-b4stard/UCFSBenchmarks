import java.util.*;
import java.lang.*;
public class Tank extends Role
{
    private String name = "Tank";
    
    
    
�加入此屬性)
    Tank(TankCT ct, String unknown_name){
        if(name.equals(unknown_name)){
            super(ct);
        }
        /*
        if ( name.equals(unknown_name) ){
            this.ct = ct;
            curAct = Action.HOLD;
            curWay = Way.WEST;
            curX = (int)(minX + maxX)/2;
            curY = (int)(minY + maxY)/2;
            tankmodel = new TankModel(ct, this, curX, curY);
        }
        */
    }
    protected void move(Action act, Way way){ }
    public int getHP(){ return HP; }
    public void setHP(){}
    public int getSpeed(){ return speed; }
    public void setSpeed(int n_speed){ speed = n_speed; }
    public int getAmm(){ return amm; }
    public void setAmm(int A_num){ amm = A_num; }
}