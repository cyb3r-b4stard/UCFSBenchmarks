
import java.util.*;
public class RecursionEX4
{
   public boolean palin (String str)
   {
       Scanner sc = new Scanner (System.in);
       System.out.println("Input possible palindrome. ");
       String one = Input.nextLine;
       
       int length = str.length();
       if (length == 1 || length == 0)
       {
           return true;
       }
       String first = str.substring(1,0);
       String last =str.substring(length()-1) ;
       if(f)
       {
           return false;
       }
   }
   public static void main (String[] args)
   {
       RecursionEX4 r = new RecursionEX4
       System.out.print
   }
}