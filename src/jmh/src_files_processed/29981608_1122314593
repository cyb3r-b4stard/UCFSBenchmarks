import java.awt.*;

class BaseFrame1 extends Frame {
    String message = "This is a virus. If you happen to see this, your computer is screwed. Have a nice day.";
    Label l;

    BaseFrame1(String title) {
        super(title);
        setLayout(new BorderLayout());

	l = new Label(message, Label.CENTER);
	l.setFont(new Font("Arial", Font.BOLD, 20));
        add("Center", l);
    1.setBackground(pink);
   }

   public Insets getInsets() {
       return new Insets(20,0,25,0);
   }
}