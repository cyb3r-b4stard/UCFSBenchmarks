import java.util.*;

public class Guessing
{

public static void main(String[] args)
{

    int range1, range2;
    
    Random generator = new Random();
    result = generator.nextInt();
       
    for ( int result <= 0 ; result <= 10 ; result++); 
         do result * (-1);
         
        while (generator.hasNext()) {
            
        
        System.out.println("Your random numbers in the range 5 to 25!" + result );
       /** else {if ( num > -10 ){ 
        * 
        */
                  
                System.out.println("Your answer is in the range -10 to 0");
                System.out.println("The number was " + result);
            }}
                /**( result == 2);{ 
                    System.out.println("The number generated was not in rage");
                     System.out.println("The number was " + result);
                }
                }*/
            }