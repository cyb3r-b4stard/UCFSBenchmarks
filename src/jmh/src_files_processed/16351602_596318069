import javax.swing.JOptionPane;

public class BeatThat{
    
    public static void main(String args[]) {
    
        
        
      
        
       int rounds;
       
       String play1 = JOptionPane.showInputDialog("Enter Player 1's name.");
       HumanPlayer player1 = new HumanPlayer(play1);
       
       String play2 = JOptionPane.showInputDialog("Enter the computer's name.");
       ComputerPlayer player2 = new ComputerPlayer(play2);
       
       
       rounds = Integer.parseInt (JOptionPane.showInputDialog("Enter the number of rounds to be played."));
       
       while (rounds >= n){
           HumanPlayer.getrollDie();
           ComputerPlayer.getrollDie();
           
           JOptionPane.showInputDialog(null, "Player 1 has name " + player1.getName + "." + "Player 1 has score " + player1.getrunningScore() ".");
           JOptionPane.showInputDialog(null, "The computer has name " + player2.getName + "." + "The computer has score " + player2.getrunningScore() ".");
           
       
     
    
            
        
        
    }
}
        
 
        
        
