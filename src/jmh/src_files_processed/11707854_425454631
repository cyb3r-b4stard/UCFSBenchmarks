import java.util.Scanner;
public class Test
{
    public static void main(String[] args)
    {
        /*Scanner in = new Scanner(System.in);
        System.out.print("How many bottles are there? ");
        int bottles = in.nextInt();
        System.out.print("How many cans are there? ");
        int cans = in.nextInt();
        System.out.printf("Bottles: %4d Cans: %4d",bottles,cans);
        */
       Scanner in = new Scanner(System.in);
       /*
       System.out.print("Enter the width of the floor: ");
       int floorWidth = in.nextInt();
       System.out.print("Enter the tile width: ");
       int tileWidth = in.nextInt();
       double gap = (floorWidth-tileWidth) % (2*tileWidth)/2.0;
       int pairs = (floorWidth-tileWidth)/(3*tileWidth);
       System.out.println("The gap should be " + gap + " inches on each side and there are " + pairs + " sets of grey and white tiles.");
       */
      System.out.print("Enter the column: ");
      int col = in.nextInt();
      System.out.print("Enter the row: ");
      int row = in.nextInt();
      if((col+row) % 1 == 0){
          System.out.println("White");
    }
    else{
        System.out.println("Black");
    }
}