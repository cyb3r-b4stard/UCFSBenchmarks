import java.io.*;
public class chimpinsabarimala
{
    public static void main()throws IOException
    {
        Buffered Readerbr=new BufferedReader(new InputStream reader (System.in));
        System.out.print("enter the valus");
        int n =Integer.paraseInt(br.readline ());
        int t=n,int d,rev=0;
        while (n>0)
        {
            d=n%10;
            if(rev==t)
            System.out.Println("palindrome");
            else
            System.out.Println("not");
        }
    }
      
