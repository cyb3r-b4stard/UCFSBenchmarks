
/**
 * ### ##### ###### ### ########## ### ### #######.
 * 
 * @###### ####### ###
 * @####### #.#
 * @##### #/##/####
 */
import java.io.*;
import java.util.Scanner;
public class MFQ
{
    PrintWriter pw;
    Scanner fs;
    ObjectQueue jobQueue = new ObjectQueue();
    /**
     * MFQ Constructor, sets up scanner and printwriter.
     * @param pw, fs
     */
    public MFQ(PrintWriter pw, Scanner fs)
    {
        this.pw = pw;
        this.fs = fs;
    }
    public void getJobs()
    {
        do
        {
            String in = fs.nextLine();
            String delims = "[ ]+";
            String[] tokens = in.split(delims);
            int var1 = Integer.parseInt(token[1]);
            int var2 = Integer.parseInt(token[2]);
            int var3 = Integer.parseInt(token[3]);
            Job var2 = (var1, var2, var3);
        }
        while (fs.hasNext());
    }
}