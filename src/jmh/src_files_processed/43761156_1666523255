
/**
 * ##### # ########### ## ##### ############## ####.
 * 
 * @###### (#### ####) 
 * @####### (# ####### ###### ## # ####)
 */
public class Zahlenspeicher
{
    
    int[] zahlenspeicher = {12, 3, 7, 19, 2, 34, 1, 189};
    
    public boolean binäreSuche (int suchZahl) {
        mitte = (int) ((rechts + links) /2);
        if (a[mitte] == i)
        return true;
        if (a[mitte] > i)
        rechts = mitte -1
        if (a[mitte] < i)
        links = mitte +1  
    }
    
}