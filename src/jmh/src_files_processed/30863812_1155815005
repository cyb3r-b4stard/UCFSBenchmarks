
public class Dist
{
    public static double distance(int a, int b, int c, int d)
    {
        int x = (c - a) * (c - a);
        int y = (d - b) * (d - b);
        int inside = x + y
        double d = Math.sqrt(inside);
    }
}