import static org.junit.Assert.*;
import org.junit.After;
import org.junit.Before;
import org.junit.Test;

/**
 * ### #### ##### ###########.
 *
 * @######  (#### ####)
 * @####### (# ####### ###### ## # ####)
 */
public class TestMission
{
	private static Coureur[] coureurs = { new Coureur("Alfred", 24),
			new Coureur("Bernard", 27), new Coureur("Claude", 19),
			new Coureur("Daniel", 31), new Coureur("Emile", 25),
			new Coureur("Fred", 28), new Coureur("Gerard", 25) };
	
	private static Temps[] temps = { new Temps(1,25,35),
	        new Temps(0,54,5), new Temps(1,2,56),
	        new Temps(18,20,30), new Temps(1,5,45),
	        new Temps(0,34,23), new Temps(1,2,56) };

	public void testGet(){
	    ClassementTemps cl=new ClassementTemps();
	    
	    for (int i=0; i<coureurs.length; i++){
	        cl.add(new Resultat(coureurs[i], temps[i]));
	    }
	    
	    assertSame("Vérification de la présence d'un élément présent une fois dans le classement", , )
	}
}