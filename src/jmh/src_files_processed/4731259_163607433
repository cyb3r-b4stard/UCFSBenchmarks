import java.text.SimpleDateFormat;
import java.util.Date;
import java.util.TimeZone;
public class PaulButton implements PushButton
{
    private long timeDate = System.currentTimeMillis();
    private static int city = 0;
    
    public void push()
    {
        city++;
    }
    public String buttonState()
    {
       Date time = new Date();
       if (city == 1)
       {
           DateFormat swag = new SimpleDataFormat("dd-MM-yy HH:mm:SS z");
           swag.setTimeZone(Timezone.getTimeZone("America/New_York"));
           String eST = swag.format(time);
           String returner = "Date and Time in New York: " + eST);

       }
       if (city == 2)
       {
           
           
       }
       if (city == 3)
       {
           
       }
       if (city == 4)
       {
           
       }
       if (city == 5)
       {
          
       }
    }
}