import.javax.swing.JOptionPane;
public class Payment{
    public static void main(String[] args){
        String str = JOptionPane showInputDialog("hour");
     int h = Integer.parseInput(str);
     if(h > = 1&&  h < = 60)}
    }else{
        Systemout.println("Error");
    }