import java.util.ArrayList;
import java.util.Collections;
public class TestCircle
{
    public static void main(String[] args)
    {
        Circle circle = new Circle();
        print(circle);
        Circle circleTwo =new Circle(10);
        print(circleTwo);
        circle.setRadius(10);
        circle.setColor("red");
        circle.setFilled(false);
        print(circle);

        ArrayList<Circle> circleList = new ArrayList<Circle>();
        circleList.add(circle());
        circleList.add(circleTwo);
        Array of Object
        Circle[] circleArray = new Circle[3];
        circleArray[0]= new Circle(50);
        circleArray[1]= new Circle(0);
        circleArray[2]= new Circle(30);
        for(int i=0; i<3;i++)
        {
            print(circleArray[i]);
        }
        Collections.sort(circleList);
        for(Circle currentCircle : circleList)
        {
            System.out.println("ArrayList: "+ currentCircle );
        }
        Rectangle rectangle=new Rectangle(4,6);
        print(rectangle);

        /* if(myObject instanceof Circle)
        {
        print(myObject);
        }*/

    }

    public static void print(Object circle)
    {
        System.out.println(circle);
    }
}