class Author
{
   private Name name;
   private Date born;
   private Date died;
   private Name pseudonym;
    
   public Author()
   {
        Name name = new Name();
        Date born = new Date();
        Date died = new Date();
   }
    
   public Author( String firstName,
                  String lastName,
                  String middleName,
                  int    yearBorn,
                  int    monthBorn,
                  int    dayBorn,
                  int    yearDied,
                  int    monthDied,
                  int    dayDied,
                  String pseudonymFirstName,
                  String pseudonymLastName,
                  String pseudonymMiddleName)
   {
        Name name = new Name(firstName, middleName, lastName);
        Date born = new Date(yearBorn, monthBorn, dayBorn);
        Date died = new Date(yearDied, monthDied, dayDied);
        if(firstName == null && middleName == null && lastName == null)
        {
            return;
        }
        else
        {
            Name pseudonym = new Name(pseudonymFirstName, pseudonymMiddleName, pseudonymLastName);
        }
   }
    /*
   public Author(Name name,
                  Date born,
                  Date died,
                  Name pseudonym)
   {
       this.died.equals(died);
       this.pseudonym.equals(null);
       
       if(name.equals(null) || (born.equals(null)))
       {
            this.name.equals(Name());
            this.born.equals(Date());
       }
       else
       {
           this.name.equals(name);
           this.born.equals(born);
       }
   }
    */
   public Author(Name name,
                  Date born,
                  Date died)
   {
        
   }
    
   public boolean isAuthorAlive()
   {
        if(died.equals(null))
        {
            return true;
        }
        else
        {
            return false;
        }
   }
    
   public int getAgeYearsOfAuthor()
   {
       return Date.CURRENT_YEAR - (Date(int year));
   }
   
}