
public class no2
{
    static int diff(int[] tableau)
    int [] modtab = new int [tableau.length];
    for(int i=0; i<tableau.length; i++)
    {
        modtab [i]= tableau 
    }
    Arrays.sort(tab);

}