import java.util.Scanner;
public class stringChecker

{
    public static void main(String[] args)
    {
        String word,find;
        
        Scanner keys = new Scanner(System.in);
        System.out.print("Enter Word");
        word = keys.next;
        System.out.print("Enter characters or strings to search for\n");
        find = keys.next();
       /* index = word.indexOf(find);
        if(index > -1)
            System.out.println("looking for " + find + " true");
        
        else
            System.out.println("looking for " + find + " false");
            */
           
       System.out.println("Looking for " + find + search(word, find);
            
}

    public static String search(String word, String find)
    {
        if(word.indexOf(find) > -1)
            return true;
        return false;
    }
    
}