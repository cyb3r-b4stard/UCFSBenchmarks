import javax.swing.*;    
 class p3_2_2	{ 
       public static void main(String args[])   { 
       float r1, r2, rsolis;
       do { r1 = Float.parseFloat (JOptionPane.showInputDialog("Ievadiet realo skaitli"; 
           r2 = Float.parseFloat (JOptionPane.showInputDialog("Ievadiet realo skaitli";
        } while (r2<=r1);
         
        do { rsolis=Float.parseFloat (JOptionPane.showInputDialog("Ievadiet realo soli";
            } while (rsolis> r2 - r1);
            
        float summa =0f;
        double reizin = 1.0; long skaits = 0L;
        for (float r= r1; r<= r2; r= r + rsolis)
       { skaits = skaits + 1L; summa= summa + r; reizin = reizin * r; };
       summa= (float)Math.round(summa * 100000.0)/100000.0f;
       reizin = Math.round( reizin *1000.0)/1000.0;
       JOptionPane.showMessageDialog(null, "Skaitļu rinda no "  + r1 + " līdz " +
       
       
           
           


      } 


 } 		
