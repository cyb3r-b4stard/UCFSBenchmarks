/**
 * ####### # ##### ###### "###" #### ######## ### ######
 */
public class Two
{
    private int x;
    private int y;

   /**
    * Creates a "Two" class with x and y coor
    * @param theX the x coord
    * @param theY the y coord
    */
    public Two(int theX, int theY)
    {
        x = theX;
        y = theY;
    }

    /**
     * Draws the red T
     */
    public void draw()
    {
      Line line1 = new Line(x, y, x+40, y); 
      Line line2 = new Line(x+20, y, x+20, y+50); 
      line1.setColor(Color.RED);
      line2.setColor(Color.RED);
      line1.draw();
      line2.draw();
      Line line2 = new line(
    }
    
}