import java.util.*;
public class StringAnalyzer
{
	private static final String VOWELS = "aeiouAEIOU";
	
      /** 	@param word contains at least 1 character
      *  		Precondition : word.length >= 1
      *  	@return true if word has more vowels than consonants
      *     @return false if word has at least as many consonants as vowels
     */
   public static boolean hasMoreVowelsThanConsonants( String word )
   {
	/* to be written in part A */int vowelCount; 
int newLength; 
for (vowelCount = 0; word.length() >= 1; vowelCount++) 
{ 
if (word.indexOf("A") != 1) { vowelCount++; } 
else if (word.indexOf("E") != 1) { vowelCount++; } 
else if (word.indexOf("I") != 1) { vowelCount++; } 
else if (word.indexOf("O") != 1) { vowelCount++; } 
else if (word.indexOf("U") != 1) { vowelCount++; } 
newLength = (word.length() - vowelCount); 
if (vowelCount > newLength) 
{ return true; } 
else { return false; } 
} 
return true; 

   }

      /** 	@param words
      *  		Precondition : words contains at least 1 String
      *      @param none
      *  		Postcondition : words will only contain Strings with more vowels than consonants
      * / 	
   public static void stringRemoverMachine( List<String> words )
   {
	/* to be written in part B */		
for(int i=0; i<words.size-1; i++)




if (!hasMoreVowelsThanConsonants(x)) {


words.remove(x);


 }

   

   public static void main(String[]args)
   {
       List<String>a=new ArrayList<String>();
       a.add("at");
       a.add("cool");
       a.add("boogie");
       List<String>b=new ArrayList<String>();
       b.add("ate");
       b.add("cool");
       b.add("frown");
       b.add("x");
       b.add("a");
       List<String>c=new ArrayList<String>();
       c.add("ant");
       c.add("cool");
       c.add("frown");
       stringRemoverMachine(a) ;
       stringRemoverMachine(b) ;
       stringRemoverMachine(c) ;
       System.out.println(a);       
       System.out.println(b);       
       System.out.println(c);       
    }
	
}
