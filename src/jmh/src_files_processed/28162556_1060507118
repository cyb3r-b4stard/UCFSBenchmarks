
/**
 * ##### # ########### ## ##### ############# ####.
 * 
 * @###### (#### ####) 
 * @####### (# ####### ###### ## # ####)
 */

import java.util.Scanner;
import java.util.ArrayList;
import java.util.ListIterator;
public class StudentGrades
{   
    public static void main(String[] args)
    {
        ArrayList<String> studentNames = new ArrayList<String>();
        ArrayList<Integer> studentGrades = new ArrayList<Integer>();
        
        Scanner scanInt = new Scanner(System.in);
        Scanner scanStr = new Scanner (System.in);
        String AddStudent = "yes";
        final String COND = "yes";
        String repeat = "yes";
        String userinput;
        int grades = 0;
        final int GRADES = 0;
     while (COND.equals(repeat) == true)
     {
            System.out.print("Please input the name of the student: ");
            studentNames.add (scanStr.nextLine());
            System.out.println("Would you like to enter another name: ");
            AddStudent = scanStr.nextLine();
             if (AddStudent == "yes")
            {
                System.out.print("Please enter another name: ");
                studentNames.add (scanStr.nextLine());
            }
            

     }
        ListIterator iteratorStudentNames = studentNames.listIterator();
        while(iteratorStudentNames.hasNext())
        {
            System.out.println(iteratorStudentNames.next());
     }
    
     ListIterator iteratorStudentGrades = studentGrades.listIterator();
      while(iteratorStudentGrades.hasNext())
     {
        System.out.println("Please enter the grades: ");
        studentGrades.add (scanInt.nextInt());
     }
     for(int count = 0; grades <= GRADES; count++)
     {
        iteratorStudentGrades.hasNext();
        System.out.println(iteratorStudentGrades.next());
     }
     for(int count = 0; grades < count; count++)
     {
         System.out.print(studentGrades);
        if(studentGrades.equals(list.get(grades)))
         {
             indexList.add(grades)
        }
        studentGrades.max(
    }
    
  }
}
    
  

  