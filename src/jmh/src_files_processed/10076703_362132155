
/**
 * ##### # ########### ## ##### ##### ####.
 * 
 * @###### (#### ####) 
 * @####### (# ####### ###### ## # ####)
 */
public class Car
{	private int xPosition;
    private int yPosition;
    private boolean isVisible;
    private Circle rearWheel;
    private Circle frontWheel;
    private Square body;
    private Square frontFender;
    private Square rearFender;

    /**
     * Constructor for objects of class Car
     */
    public Car(int xPos, int yPos)
    {
        
        (body = new Square(xPos, yPos, 30));
        body.makeVisible();
    }

    /**
     * Make this car visible.
     */
    public void makeVisible()
    {

    }

    /**
     * Make this car invisible.
     */
    public void makeInvisible()
    {

    }
    
    /**
     * Move the car horizontally by 'distance' pixels.
     */
    public void moveHorizontal(int distance)
    {

    }

    /**
     * Drives the car 10 times the amount given
     * @param amount  how much to move each step
     */
    public void drive(int amount)
    {
        for (int i=0; i < 10; i++)
        {
            moveHorizontal(amount);
        }
    }

    /**
     * Waits for the specified number of milliseconds.
     * @param  milliseconds  the number of milliseconds to delay
     */
    public void pause(int milliseconds)
    {
        if(isVisible) {
            Canvas canvas = Canvas.getCanvas();
            canvas.wait(milliseconds);
        }
    }

}