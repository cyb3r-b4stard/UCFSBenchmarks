import java.util.Scanner;
import java.io.*;
import java.util.ArrayList;

/**
 * ##### # ########### ## ##### #### ####.
 * 
 * @###### (#### ####) 
 * @####### (# ####### ###### ## # ####)
 */
public class Game
{
    ArrayList <String> dictionary;
    WordFamilies f = new WordFamilies();
    String longest = "";
    int maxLength
    
    public Game (ArrayList<String> a) {
        Scanner S = new Scanner(System.in);
        dictionary = a;
        longest = dictionary.get(0);
        
        for (int count = 1; count < dictionary.size(); count++) {
            if ( (dictionary.get(i)).length() < (dictionary.get(i-1)).length()) {
                longest = dictionary.get(i-1);
            }
        }
        maxLength = longest.length();
        
        System.out.println("Welcome to Hangman!");
        System.out.println(" ");
        System.out.print("Enter the length of the word ");
        System.out.print("that you want to guess: ");
        int length = S.nextInt();
        System.out.println(" ");
        
        for (int count = 0; count <= 5; count++) {
            System.out.println("Enter a letter to guess.");
            char guess = S.next(".").charAt(0);
            f.trim(dictionary); 
        }
    }
    
    public void errorMessage() {
        System.out.println("That entry is invalid.");
        System.out.println("Let's try again!");
    }
}