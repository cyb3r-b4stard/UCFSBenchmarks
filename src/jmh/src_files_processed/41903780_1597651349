import javax.swing.JOptionPane;

public class P1F
{
    public static void main()
    {
        String answerYears = JOptionPane.showInputDialog("How many years do you want to invest?");
        String answerAge = JOptionPane.showInputDialog("How old are you?");
        String answerRate = JOptionPane.showInputDialog("What is the expected rate?");
        
        int numYears = Integer.parseInt(answerYears);
        int numAge = Integer.parseInt(answerAge);
        int numRate = Integer.parseInt(answerRate);
        
        int yearArray = new [numYears];
    }
}