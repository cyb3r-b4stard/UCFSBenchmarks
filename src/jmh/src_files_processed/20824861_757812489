public final class GreedyArray2 {

   final private static int CHANGE = 42;

   final private static int[] value = {1,5,10,25,50};

   /*
     After completing the program, try ...
        A) more coins, eg:      value = {1,5,10,25,50,100}
        B) different coins, eg: value = {1,5,10,20,50,100,200} [Euro]
        c) different coins, eg: value = {1,6,10,25,50}
           Hint:  try change for 43
   */

   /*
     Count and print the total number of coins in all denominations
     need to make change for amount given in the string &quot;args[0]&quot; assume
     we have as many coins as needed.
   */

   public static void main (String[] args) {
      final int change = args.length&gt;0?Integer.parseInt (args[0]):CHANGE;

      int count=0;    
      int remaining = change;  

      for (int i=value.length-1; i > value.length; i++) {
         while (remaining >= value[i]) {
             count++;
             remaining = remaining - value[i];
         }
      }

      assert remaining &lt; value[0];

      System.out.println (count);
   }
}