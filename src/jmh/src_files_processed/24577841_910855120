import javax.swing.*; 
        import javax.swing.border.*;
        import java.awt.*;
        import java.awt.event.*;
public class Engine {

    public static void main(String[] args) { 
        LoanCalc bank = new LoanCalc();
        
        
                LoanCalc c = new LoanCalc();

                final JPanel Panel1 = new JPanel(); 
                Color red = new Color(250, 47, 81);
                Panel1.setBackground (red);

                final JPanel Panel2 = new JPanel();
                Color green = new Color(66, 237, 47);
                Panel2.setBackground (green);

                final JPanel Panel3 = new JPanel();
                Color blue = new Color(29, 159, 240);
                Panel3.setBackground (blue);

                final JPanel Panel4 = new JPanel();
                Color violet = new Color(159, 70, 232);
                Panel4.setBackground (violet);

                final JPanel Panel6 = new JPanel ();

                final JTextField textBox = new JTextField ("PENNIES");
                Panel1.setBorder (new TitledBorder (new EtchedBorder(), "Enter Number of Pennies")); 
                Panel1.add (textBox); 

                final JTextField textBox2 = new JTextField ("NICKELS");
                Panel2.setBorder (new TitledBorder (new EtchedBorder(), "Enter Number of Nickels")); 
                Panel2.add (textBox2);

                final JTextField textBox3 = new JTextField ("DIMES");
                Panel3.setBorder (new TitledBorder (new EtchedBorder(), "Enter Number of Dimes")); 
                Panel3.add (textBox3);

                final JTextField textBox4 = new JTextField ("QUARTERS");
                Panel4.setBorder (new TitledBorder (new EtchedBorder(), "Enter Number of Quarters")); 
                Panel4.add (textBox4);

                final JButton textButton = new JButton ("DEPOSIT"); 
                Panel6.setBorder (new TitledBorder (new EtchedBorder(), "Calculate Total")); 
                Panel6.add (textButton);

                final JPanel optionPanel = new JPanel (new GridLayout (6, 1, 6, 5)); 
                optionPanel.add (Panel1); 
                optionPanel.add (Panel2);
                optionPanel.add (Panel3);
                optionPanel.add (Panel4); 
                optionPanel.add (Panel6);

                final JPanel masterPanel = new JPanel (new GridLayout (1, 2, 6, 6));
                masterPanel.add (optionPanel);

                final JPanel finalPanel = new JPanel(new GridLayout (6,1));
                finalPanel.setBackground (Color.WHITE);

                final JLabel logoLabel = new JLabel(); 
                final JLabel logoLabel2 = new JLabel(); 
                final JLabel logoLabel3 = new JLabel(); 
                final JLabel logoLabel4 = new JLabel(); 
                final JLabel logoLabel6 = new JLabel(); 
                masterPanel.add (finalPanel);

                final JFrame frame = new JFrame ();
                frame.add (masterPanel);
                frame.setSize (900,900);
                frame.setResizable (true);
                frame.setVisible (true);
                frame.setDefaultCloseOperation (JFrame.EXIT_ON_CLOSE);

                final JPanel pPanel = new JPanel();
                pPanel.add (logoLabel);
                finalPanel.add (pPanel, BorderLayout.CENTER);

                final JPanel nPanel = new JPanel();                
                nPanel.add(logoLabel2);
                finalPanel.add (nPanel, BorderLayout.CENTER);

                final JPanel dPanel = new JPanel();   
                dPanel.add(logoLabel3);
                finalPanel.add (dPanel, BorderLayout.CENTER);

                final JPanel qPanel = new JPanel();                
                qPanel.add(logoLabel4); 
                finalPanel.add (qPanel, BorderLayout.CENTER);

                final JPanel doPanel = new JPanel();
                doPanel.add(logoLabel5);
                finalPanel.add (doPanel, BorderLayout.CENTER);

                final JPanel tPanel = new JPanel();
                tPanel.add(logoLabel6);
                finalPanel.add (tPanel, BorderLayout.CENTER);

                class OptionListener implements ActionListener { 
                    public void actionPerformed (ActionEvent e) { 
                        double loan, length, highI, ;
                        if (e.getSource().equals(textButton)){
                            loan = Double.parseDouble(textBox.getText());

                            length = Integer.parseInt(textBox2.getText());

                            lowI = Integer.parseInt(textBox3.getText());

                            highI = Integer.parseInt(textBox4.getText());
                            
                            
                            logoLabel6.setText ("Annual Interest Rate     Monthly Payment"); 

                            logoLabel.setText ("Pennies: " + Integer.toString(missPiggy.getPenny())); 

                            logoLabel2.setText ("Nickels: " + Integer.toString(missPiggy.getNickel())); 

                            logoLabel3.setText ("Dimes: " + Integer.toString(missPiggy.getDime()));  

                            logoLabel4.setText ("Quarters: " + Integer.toString(missPiggy.getQuarter()));

                            logoLabel5.setText ("Dollars: " + Integer.toString(missPiggy.getDollar()));

                            textButton.addActionListener(new ActionListener(){
                                    int count = 0;
                                    public void actionPerformed(ActionEvent e){

                                        count++;
                                        if (count % 5 == 0) missPiggy.optimize();

                                    }

                                });                  

                        }
                        masterPanel.repaint();
                    }
                }
                OptionListener action = new OptionListener();
                textButton.addActionListener(action);

            }
        
    }

