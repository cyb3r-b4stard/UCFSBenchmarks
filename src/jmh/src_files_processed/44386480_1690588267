import java.util.Scanner;
public class techLibrary{
   public static void main (String[] args){  
     Scanner input = new Scanner(System.in); 
     String title, category, deweyDecimal, choice;
     system.out.println("Enter the book's Title:");
     title = input.NextLine();   
     system.out.println("Enter the book's category:");
     category = input.NextLine();
     if (category=="theories" || category=="Philosophy");  deweyDeciaml = "600-601";{ }
 else{ if (category=="Dicitionary" || category=="Encyclopedia");{
        deweyDecimal = "600-603";
    }
else  system.out.println("This book is not a part of the technology library.");
        system.out.println("Add this book to the library? (y/n):");
    choice = input.NextLine();
     choiceChar = Choice.charAt(0);
     switch(choiceChar){
        case 'y': System.out.println("The book," +title "has been added to the library.");
        system.out.println("Dewey Decimal Code:" +deweyDecimal); break;
        case 'n': System.out.println("Adding of book has been cancelled."); break;
        default: System.out.println("Invalid Choice!"); break;
         }
        }
    }