
/**
 * ##### # ########### ## ##### ########### ####.
 * 
 * @###### (#### ####) 
 * @####### (# ####### ###### ## # ####)
 */
import javax.swing.*;
import java.awt.*;
import java.awt.event.*;
public class Temperature
{
   private static final int WIDTH = 600;
   private static final int HEIGHT = 400;
   
   private JLabel celsiusL, fahrenheitL; 
   private JTextField celsiusTF, fahrenhietTF;
   private JButton convertB, exitb;
   
   private ConvertButtonHandeler cbHandeler;
   private ExitButtonHandeler ebHandeler;
   
   public Temperature()
   {
     setTitle("convert temperature");
     
     celsiusL = new JLabel ("enter the celsius;", SwingConstants.RIGHT);
     fahrenhietL = new JLabel ("enter the fahreinhiet;", SwingConstant.RIGHT);
     
     
     fahrenhiet = new JLabel ("enter fahrenhiet");
     celsius = new JLabel ("enter celsius");
     
     celsiusTF = new JTextfield(10);
     fahrenhiet = new JTextfield(10);
     
     
     convertB = new JButton("conert");
     cbHandeler = new ConvertButtonHandeler();
     convertB.addActionListener(cbHandeler);
     
     exitB = new JButton("Exit");
     ebHandeler = new ExitButtonHandeler();
     exitB.addActionListener(ebHandeler);
     
     Container Pane = getContentPane();
    
     Pane.add(celsiusL);
     Pane.add(celsiusTF);
     Pane.add(fahrenheitL);
     Pane.add(fahrenhietTF);
     
    setSize(WIDTH, HEIGHT);
    
    setDefaultCloseOperation(EXIT_ON_CLOSE);
    setVisible(true);
    }
  private class ConvertButtonHandeler implements ActionListener
  {
      public void actionPerformed (ActionEvent e)
      {
           double width, length, area, perimeter;
        
        celsius = (5.0 / 9.0 * ( fahreinhiet - 32);
        fahreinhietTF = Double.parseDouble(fahreinhietTF.getText());
       
  
        
        
        
    }
}
private class ExitButtonHandler implements ActionListener
{
    public void actionPerformed (ActionEvent e)
    {
        System.exit(0);
    }
}      
    public static void main(String[] args)
    {
       RectangleProgramOne rectProg = new 
       RectangleProgramOne();
    }
}
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    }
}