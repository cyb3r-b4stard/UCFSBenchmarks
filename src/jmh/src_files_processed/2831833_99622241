import java.io.*;
import java.util.Iterator;
import java. util.Scanner;
/**
 * ##### # ########### ## ##### #### ####.
 * 
 * @###### (#### ####) 
 * @####### (# ####### ###### ## # ####)
 */
public class Main
{
    /**
     * An example of a method - replace this comment with your own
     * 
     * @param  y   a sample parameter for a method
     * @return     the sum of x and y 
     */
    public void main(String fichier){
        Scanner scan = new Scanner (new File (fichier));
        
        int count=1;
        while (scan.hasNextLine()){
            while (scan.hasNext()){
                Mot mot = new Mot (scan.next(),);
                if (mot.estIndexe()){
                    
                }
            }
            count++;
        }
    }
    
    /**
     * Methode qui verifie si un mot est déjà dans l'index
     */
    public boolean estIndexe(String mot){
        Iterator<Mot> iter = Index.getIterator();
        while (iter.hasNext()){
            if (mot.equals(Mot.getMot())){
                return true;
            }
            return false;
        }
    }
}
