
/**
 * ##### # ########### ## ##### ############ ####.
 * 
 * @###### ####### ######; ####### ##: ######## 
 * @####### #.#; ## ####### ####
 */

import java.util.Scanner;        
import javax.swing.JOptionPane;     

public class FramingGreat
{

    static Scanner input = new Scanner(System.in);

    public static void main(String args[])
    {

        double frameWidth;
        double frameHeight;
        double frameArea;
        double framePrice;
        int frameChoice;
        char glassChoice;
        double totalCost;
        String quotationString;

        final double REGULAR_COST = 0.15;
        final double VINTAGE_COST = 0.20;
        final double FANCY_COST = 0.25;
        final double MINIMALIST_COST = 0.45;
        final double CARDBOARD_BACKING_PAPER_COST = 0.02;
        final double GLASS_COST= 0.05;

        boolean flag=false;

        System.out.print("Please enter the frame dimensions in inches (width height):");   
        frameWidth = input.nextDouble();
        frameHeight = input.nextDouble();
        System.out.println();

        frameArea = frameWidth*frameHeight;

        while (flag!= true)
        {
            System.out.println("Enter the type of frame: 1- regular; 2-vintage; 3-fancy; 4-minimalist");
            System.out.print("Choice?");
            frameChoice = input.nextInt();
            if (frameChoice == 1)          
            {
                framePrice = REGULAR_COST;
                flag = true;
            }           
            else if (frameChoice == 2)      
            {
                framePrice= VINTAGE_COST;
                flag = true; 
            }            
            else if (frameChoice ==3){       

                framePrice = FANCY_COST;
                flag = true;
            }           
            else if (frameChoice ==4)   
            {
                framePrice = MINIMALIST_COST;
                flag = true;
            }           
            else        
            {
                System.out.println("Invalid selection, please try again");
                System.out.println("Enter the type of frame: 1- regular; 2-vintage; 3-fancy; 4-minimalist");
                System.out.print("Choice?");
            }
        }
        System.out.println("Do you want glass on top? Y/N");
        System.out.print("Choice?");

        int glassChoice=nextChar();
        boolean glass
        if (glassChoice = y or Y);
        {
            totalCost = (framePrice * frameArea) + (framePrice * CARDBOARD_BACKING_PAPER_COST) + (frameArea * GLASS_COST);
        }
        else
        {
            totalCost = (framePrice * frameArea)  + (framePrice * CARDBOARD_BACKING_PAPER_COST);
        }



        quotationString = String.format("****** Quotation ******")
        + String.format ("%n")
        + String.format("The toal cost of framing is €")
        + String.format ("%n");

        JOptionPane.showMessageDialog(null,quotationString,"Framing Great", JOptionPane.PLAIN_MESSAGE);
        System.exit(0);

    }

}
