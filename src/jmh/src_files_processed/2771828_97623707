
/**
 * ##### # ########### ## ##### ################ ####.
 * 
 * @###### (#### ####) 
 * @####### (# ####### ###### ## # ####)
 */
import java.io.File;
import java.io.PrintWriter;
import java.util.Scanner;

public class IoChallenge2Test
{

    /**
     * Constructor for objects of class IoChallenge2Test
     */
    public static void main (String[] args)
    {
        File filename;
        Scanner record;
        filename = new File("QuarterlySales.txt");
        record = new Scanner(filename);
   
   
        boolean invData;
  
        if (filename.isFile())
        {
            invData = readFile(record);
        }
        else
        {
            System.out.println("File doesn´t exist" + filename);
        }
        
    }

    /**
     * An example of a method - replace this comment with your own
     * 
     * @param  y   a sample parameter for a method
     * @return     the sum of x and y 
     */
    public static boolean readFile(Scanner inpRec) 
    {
        double sales;
        int quarter;
        String division;
        boolean inv;
        while (inpRec.hasNext())
        {
          division = inpRec.next();
          quarter = inpRec.nextInt();
          sales = inpRec.nextDouble();
          inv = validate(division, quarter, sales);
          if (inv == true)
          {
            }   
        }
        return x + y;
    }

    /**
     * An example of a method - replace this comment with your own
     * 
     * @param  y   a sample parameter for a method
     * @return     the sum of x and y 
     */
    public static boolean validate(String division,int quart, double value) 
{
}
