
import java.awt.*;
import javax.swing.*;
import java.awt.event.*;
public class battleCardsWindow extends JFrame {
    private JButton start, instructions, characterSheet;
    private JLabel title;
    JPanel panel = new JPanel();
    panel.setLayout(null);
    
    public battleCardsWindow() {
        super("Battle Cards"); 
        setLayout(new FlowLayout());
        setSize(800, 700);
        setDefaultCloseOperation(EXIT_ON_CLOSE); 
        
        add(panel);
    }
    
    public void paint(Graphics g) {
        
    }
}
    