import java.util.ArrayList;

/**
 * ######## ### ##### ## ####### ## ## ##### ###### ### ### ####### 
 * ############## # ### ####### ## #######
 * 
 * @###### (...) 
 * @####### (...)
 */
public class Oficina
{
      ArrayList <Seguro> seg;

    /**
     * Constructor que crea la lista vacia
     */
    public Oficina()
    {
        seg==new ArrayList <Seguro> ()
    }
    
    /**
     * Busca si en la lista existe un seguro con la misma matricula que
     * matricula. Si se encuentra un coche de la misma matricula se debe 
     * retornar false. 
     * En caso contrario se crea un nuevo seguro usando los datos del 
     * coche que se pasan como parametros y se establece su prima
     * 
     */
    public boolean anadeCoche(String matricula, String modelo, 
        int potencia)
    {
    return false;
    }

    /**
     * Busca si en la lista existe un seguro para la matricula que 
     * se pasa como parametro (utilizar para ello uno de los esquemas
     * de busqueda vistos en clase). Si se encuentra, aumenta su 
     * numero de siniestros, aumenta su prima en un 10%, y retorna
     * su numero de siniestros ya actualizado. En caso contrario, 
     * retorna -1 para indicar el error
     */
    public int anadeSiniestro(String matricula) {
    return -1;
    }

    /**
     * Hace un listado en pantalla de todos los seguros de la oficina, 
     * cada uno con su matricula, modelo y potencia, su numero de 
     * siniestros y su prima. Un seguro por linea
     */
    public void listado() {

    }

    /**
     * Retorna un array con las matriculas de los peores clientes, que
     * son  aquellos con un numero de siniestros igual al maximo
     */
    public String[] peoresClientes() {
    return null;
    }
    
    private void busca(){
    }
}