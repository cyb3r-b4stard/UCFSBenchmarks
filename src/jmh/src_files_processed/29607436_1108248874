import javax.swing.JOptionPane;
public class NameSort
{
    public static void main(String [] args)
    {
        String name[] = new String[11];
        int age[] = new int[11];
        int pay[] = new int[11];
        int pointer[] = new int[11];
        String output = "";
        for(int count = 1; count <= 2; count++)
        {
            name[count] = JOptionPane.showInputDialog("Enter in the name.");
            age[count] = Integer.parseInt(JOptionPane.showInputDialog("Enter in the age."));
            pay[count] = Integer.parseInt(JOptionPane.showInputDialog("Enter in the pay."));
            pointer[count] = count;
            output = output + name[count] + "/n" + age[count] + "/n" + pay[count] + "/n";
        }
        for(int count = 1; count == 1; count++)
        {
            for(int count2 = 2; count2 == 2; count2++)
            {
                if(name[count].compareTo(list[count2])
                {

                }
            }
        }
    }
}



