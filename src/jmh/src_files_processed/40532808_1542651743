import java.util.Scanner;
 
public class Test
{
    public static void main(String[ ] args)
    {
        Scanner keyboard;
        keyboard = new Scanner(System.in);
        System.out.println("Enter an integaer between 1 and 30");
        int number = keyboard.nextInt();
        int i = 1;
        int x = 2;
        if (number > 30 || number < 1)
        {
            System.out.println("Could you please input a number inside the range of 1-30");
        }
        else
        while (i != number)
        {
            temp = i + x;
            x++;
            temp + x = temp;
            i++;
        }
        
        if (sum > 100)
        {
            System.out.println("The sum is " + sum);
            System.out.println("AWESOME!");
        }
        else
        System.out.println("The sum is " + sum);
    }
}
        
        