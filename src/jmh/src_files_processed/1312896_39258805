public class AverageRunner
{
    public static void main()
    {
        Averager myAv=new Averager();
        myAv.registerValue(5);
        myAv.registerValue(2);
        myAv.registerValue(7);
        System.out.println("The average of the " +myAv.getCount()+" numbers entered so far " +myAv.getAverage());
        myAv.registerValue(4);
        myAv.registerValue(6);
        System.out.println("The average of the " +myAv.getCount()+ " numbers entered do far " +myAv.getAverage());
    