/**
 * ######## #### #######
 * ##### #####
 * 
 * ##### ## # ######## #### ## ####
 */
import java.util.Scanner;
public class VariableChecker
{
    public static void main (String [] args)
    {
        Scanner stdIn = new Scanner (System.in);
        String variableName;
        int i;
        boolean white;
        white = false;
        boolean bad;
        bad = false;
        public String substring(int beginIndex, int afterEndIndex);

        System.out.println("This program checks the properness of a proposed Java variable name."); 
        for (i = 1; i < afterEndIndex; i++)
        {
            if (Character.isWhitespace)
            {
               white = true; 
            }
        }
        if (charAt0 > 0)
        {
            white = true;
        }
        if (Charactercharat0.isLowercase)
        {
            bad = true;
        }
        if (white)
        {
            System.out.println("Illegal.");
        }
        else if (bad)
        {
            System.out.println("Legal, but uses poor style.");
        }
        else
        {
            System.out.println ("Good!");
        }
    }
}