import java.util.Random; 
import java.util.*;

public class Array_mischen { 
public void Interval (double inf,double sup)
{ inf=26;
  sup=1;
}
    private static int[] arrayMix(int[] zahlen) { 
        int tmp; 
        int rand; 
        Random r = new Random(); 
        for (int i = 0; i < zahlen.length; i++) { 
            rand = r.nextInt(zahlen.length); 
            tmp = zahlen[i]; 
            zahlen[i] = zahlen[rand]; 
            zahlen[rand] = tmp; 
        } 
        return zahlen; 
    } 

    public  void starten ()
    { 
        int[] zahlen = { Interval (double inf,double sup) }; 
        zahlen = arrayMix(zahlen); 
        for (int i : zahlen) 
            System.out.println(i); 
    } 
} 