
/**
 * ######### ### ####### ########### ## #########. 
 * 
 * @##### ####
 * @### ## ####
 */

/**
 * ###### ######## #########
 * 
 */public class Musician
{

    private String firstName;
    private String lastName;
    private String primaryInstrument;
    private String phoneNumber;
    private String email;
    private int numberAwardsReceived;
    private boolean underContract;
    
    
    

    /**
     * Constructor for objects of class Musician
     * 
     */
    /**
     * Default constructor
     * 
     */public Musician()
    {
        x = 0;
    }
    /**
     * Constructor to define parameters
     * @param _firstName is the musician's first name as a string
     * @param _lastName is the musican's last name as a string
     * @param _primaryInstrument is the musician's primary instrument as a string
     * @param _phoneNumber is the musican's phone number as a string
     * @param _email is the musician's email as a string
     * @param _numberAwardsReceived is the musican's number of awards, as an integer
     * @param _underContract is a boolean that is TRUE if the artist is under contract. Default will be FALSE.
     */
    public Musician (String _firstName, String _lastName, String _primaryInstrument, String _phoneNumber, String _email,int _numberAwardsReceived, boolean _underContract) {
        firstName = _firstName;
        lastName = _lastName;
        primaryInstrument = _primaryInstrument;
        phoneNumber = _phoneNumber;
        email = _email;
        underContract = _false;
    }
    
    /**
     * @return returns firstName as String
     */
    public int get (String firstName);
    {
        return firstName;
    }
    /**
     * @param gets input from user to assign to firstName as String
     */
    public void setfirstName (String input);
    {
        firstName = input;
    }

    /**
     * ensure the firstName is not null or empty
     */
    public void notNull(int field.size);
    
    {if (field.size() == 0);
    {system.out.println("The field is empty.");
    }
}
    /**
     * displayfirstName() prints out all the information for the course that the sudent is enrolled in. 
     */
    public void displayfirstName()
    {
       /**
     * displayCourseInfo() prints out all the information for the course that the sudent is enrolled in. 
     */
}
    public void displayfirstName()
    {
        System.out.println("First Name: " + firstName);
        System.out.println("Last Name: " + lastName);
        
        
        if (required == true)
        {
            System.out.println("Required?: Yes");
        }
        else
        {
            System.out.println("Required?: No");
        
        
        
        {
        System.out.println("Course Name: " + courseName);
        System.out.println("Course Number: " + courseNumber);
        System.out.println("CNumber of Credits: " + numberOfCredits);
        
        /**
         * @if detect empty courseName fields
         */
        if (field.size() == 0) {
            System.out.println("The field is empty.");
        }
            
       