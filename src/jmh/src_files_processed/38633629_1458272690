/**
 * #######
 * ############ (## ######)
 * ############## ### ### ####### ####################### ##### ##### #######-###########.
 *  
 */
public class CommonPrefix {

    /**
     * Gibt die Länge des gemeinsamen Präfixes der gegebenen Zeichenketten
     * zurück. Falls es kein gemeinsames Präfix gibt (das schließt auch die
     * Fälle ungültiger Parameterwerte ein), so wird 0 zurückgegeben.
     *
     * @param a die erste Zeichenkette
     * @param b die zweite Zeichenkette
     * @return die Länge des gemeinsamen Prefixes
     */
    public static int getLengthOfCommonPrefix(String a, String b) {
        if(a!=null && b!= null)
        {
            int count=0;
            while(count<a.length() && count<b.length())
            {
                if(a.charAt (count)!=b.charAt(count))
                {
                    break;
                }
                else
                {
                    count++;
                }
            } 
        }return count;
    }}
    else
    {
        return 0;
    }
}}