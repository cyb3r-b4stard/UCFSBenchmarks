import javax.swing.*;
import java.awt.*;
import java.io.*;

public class AnimationPanel extends JPanel 
{
    BufferedImage pManBase = SpriteImages.getSprite(SpriteImages.PACMAN_BASE);
    BufferedImage [] pacManup = new BufferedImage[2];
    BufferedImage pacManup = {pManBase,SpriteImages.getSprite(SpriteImages.PACMAN_UP)}
    Character Pman = new Character(
    public AnimationPanel()
    {
      setBackground(Color.black);  
      setPreferredSize(new Dimension(300, 300));
    }
    
}