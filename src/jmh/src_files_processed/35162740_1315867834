
/**
 * ########### ### #### ### ###### #############_#####.
 * 
 * @###### (### ####) 
 * @####### (#### ############## #### ### #####)
 */
public class Einkaufsliste_Array
{
    private Artikel[] einkaufsliste;

    /**
     * Konstruktor für Objekte der Klasse Einkaufsliste_Array
     */
    public Einkaufsliste_Array(int anzPositionen);
    {
        int i=0;
        {
            einkaufsliste=new Artikel[anzPositionen];
        }
    }
        /**
         *  fügt den übergebenen Artikel an der ersten freien Stelle im Array ein.
         *  Sollte der Artikel bereits im Array enthalten sein, so kann er nicht erneut eingefügt werden. 
         *  In diesem Fall wird eine entsprechende Mitteilung auf der Konsole ausgeben.
         *  Ist das Array komplett gefüllt, so wird ebenfalls eine entsprechende Meldung auf der Konsole ausgegeben.
         *  Kann der Artikel eingefügt werden, so gibt die Methode true zurück, ansonsten false.
         */
        public boolean fuegeEin(Artikel a)

        {
        for(int i=0; i<einkaufsliste.length; i++);

        if(einkaufsliste[i] == a)
        {
            System.out.println(" Artikel " + a.gibInfo() + " ist schon vorhanden ");
            return false;
        }

    }
}