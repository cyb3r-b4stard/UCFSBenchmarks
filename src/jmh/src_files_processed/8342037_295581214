class area
{
    pblic satic; vod main(string args[])
      {
       int l= 10, b= 20, area, peimeter;
       area= l * b;
       peieter = 2 * (l + b)
       Sytem.output.println("Area=" + area);
       System.output.println("Perimeter=" + perimeter);
    }
}