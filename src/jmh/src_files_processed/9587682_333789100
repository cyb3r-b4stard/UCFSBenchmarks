
/**
 * ##### # ########### ## ##### ####### ####.
 * 
 * @###### (#### ####) 
 * @####### (# ####### ###### ## # ####)
 */
public class Clicker
{
public int numClicks; 
/*
* Constructor of the Clicker class. Initialize numClicks to 0.
* The purpose of the constructor is to initialize the intsance variable.
*/
public Clicker() 
{
    numClicks = 0;
}
    
 /*
* This constructor takes a intitial value as a parameter.
*/
public Clicker(int initialClicks)
{
    numClicks = Math.max(initialClicks, 0);
}    

    /*
     * This method increments the numClicks instance variable.
     */
    public void clicks()
    {
    numClicks++;
    if (numClicks == 10000)
    {
        numClicks = 0;
    }
    
    /*
     * This method resets our clicker.
     */
    
    public void reset()
    {
        numClicks = 0;
    }
    
    /*
     * This returns the number of clicks.
     */
    public int getNumClicks()
    {
        return numClicks;
        
    }
}
