import javax.swing.JOptionPane;
public class CaixaDeDialogoVetor
{
    public static void main(String []Args){
        String Quantidade=null;
        int quant;
        int x[];
        int soma=0;
        double media;
        quant=Integer.parseInt();
        JOptionPane.showInputDialog(null,"Diga a quantidade");
        quant= JOptionPane.showConfirmDialog(null,"A quantidade é="+quant+"?");
        new x[] = Integer.parseInt(args[0]);
        for(int i=0; i<quant; i++){
        
        JOptionPane.showInputDialog(null,"Diga o valor:");
        x[i]=integer.parseInt(JOptionPane.showInputDialog);
        soma+=x[i];
        
        
        
        }
    media=soma/quant;
    
    
    }
    
}