import java.util.Scanner;

public class Paint

{

    public static void main(String[] args)

    {
        Scanner scan = new Scanner(System.in);
        int length = new Scanner(System.in);
        int width = new Scanner(System.in);
        int height = new Scanner(System.in);
        double totalSqFt = length * width * height;
        double paintneeded = totalSqFt / 350;
        
        double celing = length * width;
        double wall1 = length * height * 2
        double wall2 = width * height * 2
        
        System.out.println("Please enter length of the first wall " + length);
        System.out.println("Plese enter the height of the second wall " + height);
        int w1input = scan.nextInt();
        double wall1input = length * height
        
        System.out.println("Please enter the length of the second wall " + legth);
        System.out.println("Please enter the width of the second wall " + width);
        int wall2input = width * height
        
        System.out.println("Please enter length of the celing " + length);
        System.out.println("Plese enter the width of the celing " + width);
        int w1input = scan.nextInt();
        double wall1input = length * height
        
    }

}