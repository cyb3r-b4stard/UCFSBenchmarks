import info.gridworld.actor.Bug;
public class MyMathBug extends Bug {
    /**
     * MyMathBug takes a number and uses it math-crunching power
     * to figure out the first 10 multiples of the number. It 
     * will then turn that many times then move like a bug. It
     * does the same for the next multiple. Once it is 
     * done it returns to the first multiple and the process
     * begins over again.
     */
    private int factor;
    private int counter:
    
    public MyMathBug(int x) {
        factor = x;
    }
    public void mathCrunching(int a){
        factor*counter
        for (int i = 1; i <= ; i++){
        }
            
            
        
    }
    public void act(){
        if (canMove){
            mathCrunching(factor*);
            super.act();
        }
    }
}