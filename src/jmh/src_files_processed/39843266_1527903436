public class Tester{
   static double A=0;
   static double B=0;
   static double C=0;
    public static  double getDist(double x, y){
        
        double Dist= Math.abs(A*x+B*y+C)/Math.sqrt(Math.pow(A,2)+Math.pow(B,2));
        return Dist;
    }
}