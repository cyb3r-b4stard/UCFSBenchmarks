
import javax.swing.JOptionPane;
import java.util.Random;

public class Player
{
    int rounds,
        roll,
        score,
        player1,
        player2;
    String name;
    int choice;
    
    public static void main(String args[ ]){
        DiceRoll player1 = new DiceRoll();
        DiceRoll player2 = new DiceRoll();
       
        boolean done = false;
        
        String[ ] choices = {"Play", "Quit"};
        
        while (!done) {
            choice = JOptionPane.showOptionDialog(
                null,
                "Click a choice",
                "Function Menu",
                JOptionPane.YES_NO_CANCEL_OPTION,
                JOptionPane.QUESTION_MESSAGE,
                null,
                choices,
                choices[0]);
                
                switch (choice) {
                    case 0:
                    int rounds = int.parseInt(JOptionPane.showInputDialog("Enter number of rounds to play ");
                    player.setrounds(rounds);
                    player.getrounds(rounds);
                    
                    name = JOptionPane.showInputDialog("Enter your name ");
                    player1.setname(name);
                    player1.getname(name);
                    player2.setname(PC);
                    player2.getname(PC);
                    
                    public int diceRoll();
                    set 
                    break;
                   
                }
        }
    }
