import java.util.Scanner;
import images.APImage;
import images.Pixel;

public class P5I
{
    public static void main(Stringp[] args)
    {
        Scanner reader = new Scanner(System.in);
        APImage turtle = new APImage("turtle.jpg");
        turtle.draw();
        
        System.out.print("Enter a positive enlargment factor: ");
        int e = reader.nextInt();       
        
        int height = turtle.getImageHeight();
        int width = turtle.getImageWidth();
        
        APImage newturtle = new APImage(width*s, height*s);
        for (int row = 0; row < height; row += (1/e))
        
    }
}