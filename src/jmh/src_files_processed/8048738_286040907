class words
{
    void display()
    {
        int i, l = s.length();
        for(i= l; i>=1; i--)
        {
            String p = s.substring(0,i),
            System.out.println(p);
        }
            
    }
}