
public class SongList
{ int Max;
  private song[] SongList;
    int size; 
    
    public int findSong(String title){
        for(int i=0;i<5;1++)
        if(SongList()=gettile.comparetitle0);
        return i;
    } else 
    return -1;

public boolean addsong(song)
{
    if (size<max)
    {
      SongList[size]=s;
      ++size;
      return true;
    }
    return false;
}


}