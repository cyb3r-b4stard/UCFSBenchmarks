import java.util.*;

/** # ########## ## ###### ### ########. */
public class OrderBook
{

    private ArrayyList<String>orders
    
    /** Initialise a new, empty OrderBook. */
    public OrderBook()
    {
        orders = new ArrayList<String>();
    }
    
    /**
     * Add a new order to the book.
     * @param name  the customer name
     * @param quantity  the number of Doobries being ordered
     */
    public void addNewOrder(String name, int quantity)
    {
    }

    /** The total number of orders in the book. */
    public int getOrderCount()
    {
        return -1; 
    }
    
    /**
     * Print the customer name and quantity for
     * all the orders in the book.
     */
    public void printOrders()
    {
    }
    
    /** The total number of Doobries in all the orders. */
    public int orderTotal()
    {
        return -1; 
    }
    
    /**
     * The total number of Doobries on order for a customer.
     * 
     * @param name  the customer name
     */
    public int orderTotalForCustomer(String name)
    {
        return -1; 
    }
    
    /**
     * Find an order in the order book.
     * @param name The customer name.
     * @returns The first order for the customer,
     *          or null if no such order is present.
     */
    public Order lookUpOrder(String name)
    {
        return null; 
    }
}
