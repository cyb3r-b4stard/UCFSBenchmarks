import javax.swing.*;
public class easter
{
    public static void main (String [] args)
    {
        int stones, take, com, you;
        stones = (int)(Math.random()*15) +15;
        System.out.println ("There are " +stones+ " stones.";
        com = (Math.random()*3)+1;
        while (stones>=0)
        {
            if (stones>=3)
            {
                System.out.println ("How many stones would you like to take (Take 1-3)?")
                take = keybaord.nextInt ();
                stones = stones-take;
                stones = stones-com;
                System.out.println ("There are " +stones+ " stones.");
                if (take>3 || take<1)
                {
                    System.out.println ("That isn't between 1 and 3. Try again.");
                    take = keyboard.nextInt ();
                    stones = stones-take;
                    stones = stones-com;
                    System.out.println ("Computer takes "+com+ ". There are " +stones+ " stones.");
                }
            }
            else if (stones==2)
            {
                System.out.println ("How many stones would you like to take (Take 1-2)?");
                take = keyboard.nextInt ();
                stones = stones-take;
                stones = stones-com;
                System.out.println ("Computer takes "+com+ ". There are " +stones+ " stones.");
                if (take>2 || take<1)
                {
                    System.out.println ("That isn't between 1 and 2. Try again.");
                    take = keyboard.nextInt ();
                    stones = stones-take;
                    System.out.println ("There are " +stones+ " stones.");
                }
            }
            else if (stones==1)
            {
                System.out.println ("How many stones would you like to take (Take 1)?");
                take = keyboard.nextInt ();
                stones = stones-take;
                stones = stones-com;
                System.out.println ("Computer takes " +com+ ". There are " +stones+ " stones.");
                if (take>1 || take<1)
                {
                    System.out.println ("That isn't 1. Try again.");
                    take = keyboard.nextInt ();
                    stones = stones-take;
                    System.out.println ("There are " +stones+ " stones.");
                }
            }
            else if (take==0)
            {
                System.out.println ("You can't pick 0. Try again.");
                take = keyboard.nextInt ();
                stones = stones-take;
                System.out.println ("There are " +stones+ " stones.");
            }
            else if (take<0)
            {
                System.out.println ("You can't pick a negative number. Try again.");
                take = keyboard.nextInt ();
                stones = stones-take;
                System.out.println ("There are " +stones+ " stones.");
                }
        }
        
            