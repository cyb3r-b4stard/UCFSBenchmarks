import java.util.Scanner;
import java.util.Random;
public class NameGenerator
{
    
    public static void main(String [] args)
    {

        
        Random random = new Random();
        String [] maleNames = {"Aaron", "Adam", "Alex", "Andy", "Brian", "Casey", "Charlie", "Cody", "Darren", "Darius", "Daryl", "Eric", "Frank", "Fred", "George" };
        String gender;
        System.out.println("Hello. I am a generator program. I will find you a new name based on your answers to my questions. Press enter to start.");
        Scanner reader = new Scanner(System.in);
        System.out.print("What is your first name? ");
        String firstName = reader.nextLine();
        System.out.print("What is your last name? ");
        String lastName = reader.nextLine();
        System.out.print("What is your gender? (male/female) ");
        gender = reader.nextLine();
        System.out.print("What one-word adjective would you use to describe yourself? ");
        String adj = reader.nextLine();
        String name = firstName = " " + lastName;
       /if(gender.equals("male")){
        System.out.println("Your name is now: Sir " + name + " the " + adj.toUpperCase);}
        else
        System.out.println("Your name is now: Lady " + name + " the " + toUpperCase.adj);*/
    
    }

   /* public static String changeName(String name)
    {
       if(gender.equals("male")){
        System.out.println("Your name is now: Sir " + name + " the " + adj.toUpperCase);}
        else
        System.out.println("Your name is now: Lady " + name + " the " + toUpperCase.adj);
    
    }
    
    public String title(String name)
    {
        name = name + random.nextInt(titles.length);
    }*/
}