import java.util.*;
public class quad
{
    public static coid main (String args [])
    {
        Scanner input = new Scanner(System.in);
        int a, b, c;
        
        system.out.printIn("enter 3 integers.");
        a = reader.nextInt();
        b = reader.nextInt();
        c = reader.nextInt();
        
        double x1, x2;
        x1 = (-b + Math.sqrt(b*b - 4*a*c))/(2*a);
        x2 = (-b - Math.sqrt(b*b - 4*a*c))/(2*a);
        
        System.out.printIn("Here are the two solutions to the equation with")
        System.out.printIn(x1 + "\t" + x2)
    }
}
