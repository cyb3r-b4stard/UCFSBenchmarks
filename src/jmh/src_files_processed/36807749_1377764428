
import java.util.*;

public class GradeBook
{
    public static void main (String []args)
    {
      int grade1 = requestGrade();
      int grade2 = requestGrade();
      int grade3 = requestGrade();
      int grade4 = requestGrade();
      int average = average4Grades(grade1, grade2, grade3, grade4);
      printLetterGrade(average);
      
      printLetterGrade(calculate4Grades(requestGrade(), requestGrade(), requestGrade(), requestGrade()));
    }
    
    public static int requestGrade()
    {
        Scanner input = new Scanner (System.in);
        System.out.println("hey");
        return input.nextInt();
    }
    
    public static int average4Grades (int g1, int g2, int g3, int g4)
    {
        return (g1+g2+g3+g4+)/4
    }
    
    /*This method should print A if the average is between 90-100, B if between 80-89, C if between 
    70-79 and F is between 0-69.  If it is less than zero or over 100, print "Error"*/  
    public static void printLetterGrade(int avg)
    {
        System.out.println("You do it");
    }
}