
/**
 * ########### ### #### ### ###### ############.
 * 
 * @###### (### ####) 
 * @####### (#### ############## #### ### #####)
 */
public class Berechnungen
{
    /**
     * Diese Methode zeigt Beispiele fŸr die Ausgabe von Zeichenketten mit Platzhaltern fŸr das Einbinden 
     * von formatierten Werten verschiedenen Typs.
     */
    public void ausgabeTest() 
    {

        int i = 123;
        System.out.printf( "|%d|%d|%n" ,       i, -i );      
        System.out.printf( "|%5d|%5d|%n" ,     i, -i );      
        System.out.printf( "|%-5d|%-5d|%n" ,   i, -i );      
        System.out.printf( "|%+-5d|%+-5d|%n" , i, -i );      
        System.out.printf( "|%05d|%05d|%n%n",  i, -i );      

        double d = 12345.678;
        System.out.printf( "|%f|%f|%n" ,         d, -d );    
        System.out.printf( "|%+f|%+f|%n" ,       d, -d );    
        System.out.printf( "|% f|% f|%n" ,       d, -d );    
        System.out.printf( "|%.2f|%.2f|%n" ,     d, -d );    
        System.out.printf( "|%,.2f|%,.2f|%n" ,   d, -d );    
        System.out.printf( "|%10.2f|%10.2f|%n" , d, -d );    
        System.out.printf( "|%010.2f|%010.2f|%n",d, -d );    

        String s = "Monsterbacke";
        System.out.printf( "%n|%s|%n", s );                  
        System.out.printf( "|%S|%n", s );                    
        System.out.printf( "|%20s|%n", s );                  
        System.out.printf( "|%-20s|%n", s );                 
    }

    /**
     * Diese Methode berechnet die Note fŸr eine PrŸfung, die sich aus drei Teilnoten
     * zusammensetzt. Die Noten sollen mit folgender Gewichtung gemittelt werden:
     * test: 20%
     * klausur: 30%
     * pruefung: 50%
     * 
     * @param  test    Note fŸr Test
     * @param  klausur    Note fŸr Klausur
     * @param  pruefung   Note fŸr Pruefung
     */
    public void berechneNote(float test, float klausur, float pruefung)
    {

        float endnote = 0.0f;
        
        endnote = (test*0.2f) + (klausur*0.3f) + (pruefung*0.5f);
        
        
        System.out.printf("Eingehende Werte:%nTest:      %.1f%nKlausur:   %.1f%nPruefung:  %.1f%n",test,klausur,pruefung);
        System.out.println("Endnote:   "+ endnote); 
       

    }

    /**
     * Diese Methode berechnet die Enfernung, die ein Auto mit einer bestimmten Menge Benzin
     * auf guten und auf weniger guten Stra§en zurŸcklegen kann. Das Ergebnis der Berechnung soll 
     * mit einem entsprechenden Begleittext auf der Konsole ausgegeben werden.
     * Der Verbrauch des Autos soll auf normalen Stra§en 8l/100km betragen, auf schlechten Stra§en 
     * 15 % mehr.
     * 
     * @param  literBenzin  Anzahl der Liter Benzin, fŸr die die Enfernung berechnet werden soll
     */
    public void berechneBenzinverbrauch(int literBenzin)
    {
    
    System.out.printf("Gute Strasse: %.2f%nSchlechte Strasse: %.2f%n",literBenzin*12.5f,literBenzin*(12.5f+(12,5f*0.15f)));
    
    
        
        
    }

    /**
     * Diese Methode berechnet die von der aktuellen Arbeitsschicht noch verbleibende Zeit in
     * Stunden und Minuten und gibt die verbleibende Zeit auf der Konsole aus. 
     * In der Fabrik wird von einem 24 Stunden Tag ausgegangen. 
     * Au§erdem soll ausgegeben werden, in welcher Schicht man sich gerade befindet. 
     * Die Zeiten fŸr die Schichten sind wie folgt:
     * Schicht 1: von 00:00 bis 7:59
     * Schicht 2: von 08:00 bis 15:59
     * Schicht 3: von 16:00 bis 23:59
     * 
     * @param  std    Aktuelle Stunde
     * @param  min    Aktuelle Minute
     */
    public void berechneRestZeitVonSchicht(int std, int min)
    {

    }
}