

import java.util.Scanner;

public class RandomGuesserPlus
{
    public static void main(String [] args)
    {
        Scanner reader = new Scanner(System.in);
        int number;
        int guess;
        int attempts = 0;
        
        System.out.println("Pick a number between 1 and 100 (inclusive) \nand the computer will try to guess it!");
        System.out.println();
        System.out.print("Enter a number: ");
        number = reader.nextInt();
       
        System.out.println();
        
        guess = 100/2;
        
        for (guess != number; guess < 100; guess ++)
        {
            attempts ++;
            if (guess < number)
            {
                System.out.println("The computer guesses: " + guess);
                System.out.println("\tComputer guess was too low!");
                guess = 100 - (100/4);
            }
            else
            {
                System.out.println("The computer guesses: " + guess);
                System.out.println("\tComputer guess was to high!");
                guess = 
            }
        }
       
        while (guess == number)
        {
            attempts ++;
            System.out.println("\nThe computer guesses: " + guess);
            System.out.println("\nThe computer got it right!");
                break;
        }
        
        System.out.println("It took the computer " + attempts + " attempts to guess my number!");
        
        int exponent;
        double k;
        
        exponent = 0;
        
        System.out.println();
          while(exponent > -1)
        {
            System.out.print("Enter an exponent [-1 to quit]: ");
            exponent = reader.nextInt();
            
            k = Math.pow(2.0, (double)exponent);
            
            if (exponent == -1)
                break;
            System.out.println();
        }
    }
   }


        