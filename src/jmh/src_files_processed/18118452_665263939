import java.io.*; 
import java.util.*; 
import java.text.*; 
import static java.lang.System.out;
public class BaseClass
{

    public static void main( String args[] ) throws IOException
    {
        Scanner sf = new Scanner(new File("C:\\Users\\tkinman\\Desktop\\temp_Larry\\customers.txt"));
        int maxIndx = -1;        String text[] = new String[1000];
        while(sf.hasNext( )){
            maxIndx++;
            text[maxIndx]=sf.nextLine();}
        sf.close();  
        LinkedListQueue q = new LinkedListQueue( );
        for(int j=1;j<maxIndx;j++){
            q.enqueue(text[j]);
        }
        
        int TotalWaitTime = 0;
        Scanner sc = new Scanner((String)q.peekFront( ));
        int firstCustArrivalTime = sc.nextInt( );
        int nextAvailTimeA= firstCustArrivalTime;
        int nextAvailTimeB= firstCustArrivalTime;
        while(!q.isEmpty())
        {
            int j=0;
            Scanner sc = new Scanner((String)q.dequeue();
            int custArrivalTime = Integer.parseInt(run[0]);
            int custHelpTime = Integer.parseInt(run[1]); 
            int dequeueTime = 0;
            if(nextAvailTimeA <= nextAvailTimeB) 
            {
                dequeueTime = nextAvailTimeA -custArrivalTime + custHelpTime;

                nextAvailTimeA = dequeueTime + custHelpTime;
            }
            else
            {
                dequeueTime = nextAvailTimeA -custArrivalTime + custHelpTime;
                nextAvailTimeB = dequeueTime + custHelpTime;
            }
            int thisWaitTime= dequeueTime - custArrivalTime;

            TotalWaitTime = TotalWaitTime + thisWaitTime;
            System.out.println(TotalWaitTime);
           
        }
    }
}