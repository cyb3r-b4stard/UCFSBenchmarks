
/**
 * ##### # ########### ## ##### ######### ####.
 * 
 * @###### (#### ####) 
 * @####### (# ####### ###### ## # ####)
 */
import java.util.*;
public class CircleApp
{
    public static void main(String[] args)
    {
        System.out.println("Welcome to Unit Circle App.");
        System.out.println("This program will help you find out the value of function 'y' on providing the value of 'x'");
        System.out.println("In addtion, you can find out the slope of the line tangentt to the curve at any point and find the area"); 
        System.out.println("covered by the tangent and the curve."); 
        System.out.println("Press 'y' to solve for y.");
        System.out.println("Press 's' to find the slope.");
        System.out.println("Press 'a' to find the area.");
        System.out.println("Press 'q' to exit the program."); 
        Scanner userInput = new Scanner(System.in);
        System.out.print("Select one of the above options: ");
        if (userInput.hasNext("y"))
        {
            Scanner xInput = new Scanner(System.in);
            System.out.print("Please enter 'x' value: ");
            if (xInput.hasNextDouble())
            {
                double xValue = xInput.nextDouble();
                System.out.println("The 'Y' value is: "+ UnitCircleCalc.YValue(xValue));
            }
        }
        else if (userInput.hasNext("s"))
        {
            Scanner xInput = new Scanner(System.in);
            System.out.print("Please enter 'x' value: ");
            if (xInput.hasNextDouble())
            {
                double xValue = xInput.nextDouble();
                System.out.println("The slope is: " + UnitCircleCalc.slope(xValue));
            }
        }
        else if (userInput.hasNext("a"))
        {
           System.out.println("For the area, it requires you to enter three different values.");
           Scanner onePoint = new Scanner(System.in);
           System.out.print("Enter the first point: ");
           double fPoint = onePoint.hasNextDouble(
           /*if (onePoint.hasNextDouble())
           {
               double fPoint = onePoint.nextDouble();
               
            }
           Scanner twoPoint = new Scanner(System.in);
           System.out.print("Enter the second point: ");
           if (twoPoint.hasNextDouble())
           {
               double sPoint = twoPoint.nextDouble();
            }
           Scanner rectangles = new Scanner(System.in);
           System.out.print("Enter the number of rectangles you want to make between the points: ");
           if (twoPoint.hasNextInt())
           {
               int nRectangles = rectangles.nextInt();
            }
           System.out.println("The area is: " + UnitCircleCalc.areaOf(fPoint, sPoint, nRectangles));
        }
        
        /*else if (userInput.hasNext("q"))
        {
            
        }
        else
        {
        }
        /*boolean wrongInput = true;
        while (wrongInput)
        {
            System.out.print("Select one of the above options: ");
            wrongInput = userInput.hasNextIn();
            /*wrongInput =! userInput.hasNext("s");
            wrongInput =! userInput.hasNext("a");
            wrongInput =! userInput.hasNext("q");*/
       /*    if (wrongInput)
            {
                System.out.println("Enter only the above options please! ");
            }
        }*/
        
        /*if (userInput = next(y))
        {
            
        }
        /*while (wrongInput)
        {
            System.out.print("Select one of the above options: ");
            wrongInput = userInput.hasNextInt();
            System.out.println("Select only above options please!");
        
            if (userInput = )
            {
                System.out.println("Enter the 'x' value: ");
            }
        }*/
        
    }
    
}