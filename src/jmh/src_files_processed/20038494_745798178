package src.work;

import org.newdawn.slick.SlickException;
import java.util.Random;
import java.util.HashSet;
import java.util.ArrayList;

/**
 * #### ##### ########## ### ##.
 * ### ## ### ##### ## # ###### ###### ### ## ## ## ######## #### ########### ###.
 * 
 * @###### ########## #####
 * @###### ######## #######
 * @###### ###### ##¶##
 * @###### ##### ####
 */
public class KI {
    
    private ArrayList<Map> arrayshots;
    
    public KI(){
        arrayshots = new ArrayList<Map>();
        
        for(int index =1; index < map.getSize() ; index++){
            for (int index2 =1; index2 < map.getSize() ; index2++){
                arrayshots.add(map[index][index2]);
            }
        }

    }
    
    /**
     * KI performs a random simple shoot.
     * 
     * @param map Current map.
     * @return Returns the FieldEntity which was shot.
     * 
     * @throws SlickException
     */
    public static FieldEntity simpleShoot(Map map) throws SlickException{
        Random rnd = new Random();
        int x = rnd.nextInt(9);
        int y = rnd.nextInt(9);
        Coordinate posshot = new Coordinate(x,y);
        
        arrayshots.add(
        
        if (arrayshots.contains(posshot)){
            Map.shootAtField(posshot);
        }
        return Map.getEntity(posshot);
    }
    
    
    /**
     * KI performs a more advanced shoot.
     * 
     * @param map Current map.
     * @return Returns the FieldEntity which was shot.
     * 
     * @throws SlickException
    */
    public static FieldEntity extendedShoot(Map map) throws SlickException{
        return null;
    }
}