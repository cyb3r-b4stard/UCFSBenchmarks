import comp102x.IO;

public class abc
   {
        IO.outputln("Enter an integer, x:");
        int x=IO.inputInt();
        IO.outputln("Enter an integer, y:");
        int y=IO.inputInt();
        int calculated_result=x*y;
        IO.outputln("Answer = "+calculated_result);
}