import java.lang.Math;

public class Application
{
    public static void main(String[] args)
    {
        Poster myPoster = new Poster();  

        float red, green, blue;

        /*
        red = 1.0F;
        green = 0.0F;
        blue = 0.0F;
        myPoster.setTileColour(0, 0, red, green, blue);

        red = 0.0F;
        green = 1.0F;
        blue = 0.0F;
        myPoster.setTileColour(0, 1, red, green, blue);

        red = 0.0F;
        green = 0.0F;
        blue = 1.0F;
        myPoster.setTileColour(1, 0, red, green, blue);
         */
        public void randomInRange(min, max) 
        {
            return Math.random() < 0.5 ? ((1-Math.random()) * (max-min) + min) : (Math.random() * (max-min) + min);
        }
        for(int x = 0; (x >=0 && x <=83); x++) 
        {

            for(int y = 0; (y >=0 && y <=58); y++) 
            {
                red = ;
                green = ;
                blue = ;
                myPoster.setTileColour(x, y, red, green, blue);
            }
        }

        myPoster.displayAndSave();  
    }

}