using java.util.ArrayList;
/**
 * Write a description of class sort here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class sort
{

    static int[] sort(int[] input){
        BinarySearchTree<int> tree = new BinarySearchTree<>();
        ArrayList<int> result = new ArrayList<>();
        for(int i : input){
            tree.insert(i);
        }
        
        
        
    }
    
    public void inOrder(BinarySearchTree<Integer> curTree, ArrayList<int> resList){
        if(curTree == null) 
            curTree = baum;
        
        if(!curTree.getLeftTree().isEmpty()){
            inOrder(curTree.getLeftTree());
        }
        
        resList.add(curTree.getContent());
        
        if(!curTree.getRightTree().isEmpty()){
            inOrder(curTree.getRightTree());
        }
    }
}