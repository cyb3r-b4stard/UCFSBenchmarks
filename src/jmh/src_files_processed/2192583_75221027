public class Tp2Partie2 {

  public static boolean nonNullEtNonVide( String chaine ) {
    boolean res = false;
   if ( chaine != null){ 
        if (chaine.length()!=0) {
        res = true;
         } 

  }
}
  public static boolean estUneLettreMinuscule( String chaine ) {
     boolean res = false;
    if ( chaine != null) {
        if (chaine.length()==1 ) {
        
        char lettre0=chaine.charAt(0);
        if (Character.isLowerCase(lettre0)) {
            
       
        res=true;
       } 
       }   
    }
   return res;
}



 

  public static boolean estUnIntervalleValide( String chaine ) {
      boolean res = false;
    if  (chaine != null) {
        if (chaine.length()==3 ) {
        char lettre0=chaine.charAt(0);
        char lettre1=chaine.charAt(1);
        char lettre2=chaine.charAt(2);
            if (Character.isLowerCase(lettre0) && Character.isLowerCase(lettre2) && lettre0<lettre2 && lettre1 =='-'){
            
        
         
       

                res=true;
   
            }    
         
        }
    }    
    
    

   
    return res;
 

 }       


  

  public static boolean estUneExpressionValide ( String chaine ) {
    
    boolean res = false;
    if  (chaine != null) {
        if (chaine.length()!=0) {
            if (chaine.length()%3==0 ) {
                for (int i=0; i>3; i++){

          chaine.substring(i,i+3/3);
          if (chaine.length()==3 ) {
              char lettre0=chaine.charAt(0);
              char lettre1=chaine.charAt(1);
              char lettre2=chaine.charAt(2);
              if (Character.isLowerCase(lettre0) && Character.isLowerCase(lettre2) && lettre0<lettre2 && lettre1 =='-'){
               
                     res=true; 
                }
          
          }
        
         else if (false) {
         }
         }
         
        
           
    }
            
}
   
}
  }

  public static boolean estDansIntervalle( char carac, String intervalle ){
        boolean res = false;
        char lettre;
        char Intervalle;
      
            
                System.out.println ("Veuillez saisir une lettre:");
              
                 Intervalle= Clavier.lireCharLn();
                 While(intervalle=2){
                     System.out.println ("Veuillez saisir une lettre:");
                     System.out.println ("Erreur: lettre invalide!");
                
                    
                    Intervalle=this estDansIntervalle();      
                 
                    
                    res=true;
                } 
                        
    return res;
        }
    }


 

      
  
   
    
       

                  
             

