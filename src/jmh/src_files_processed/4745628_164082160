public class Casino
{
    Muenze meineMuenze;
    Player meinPlayer;
    public Casino()
    {
        meineMuenze = new Muenze();
        meinPlayer = new Player(); 
    public spielen() 
    {
        if (spielertipp == meineMuenze. String muenzwurf()){
         system.out.println("Gewonnen");   
        } 
        else {
            system.out.println("Verloren");
        }
    }
}