import java.awt.*; 
import java.awt.event.*; 
import javax.swing.*;
import java.util.ArrayList;

public class Memory extends JFrame implements ActionListener {
    private JButton btnRestart;
    private ArrayList<String> tall = new ArrayList<String>{"1", "2", "3"};
    
    public Memory() {
        super("Memory");
        JPanel pnlCenter = new JPanel (new GridLayout (4, 4));
        int i = 0;
        for (i = 0; i < 16; i++) {
            JButton btnBlank = new JButton(" ");
            pnlCenter.add(btnBlank);
            btnBlank.addActionListener(this);
        }
        add(pnlCenter, BorderLayout.CENTER);
        
        JPanel pnlSouth = new JPanel (new GridLayout (1, 1));
        
        btnRestart = new JButton ("Start på nytt!");
        
        btnRestart.addActionListener(this);
        
        pnlSouth.add(btnRestart);
        
        add(pnlSouth, BorderLayout.SOUTH);
        
        setSize(400,200);
        setVisible(true);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    }
    
    public void actionPerformed (ActionEvent event) {
        
    }
}