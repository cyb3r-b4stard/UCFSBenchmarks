public class ex2b
{
    public static boolean verifierMul(int[] L, int n)
    {
        computeur=0;
        for( int i=0; i< tab.length; i++)
        if( val%i==0)
        compteur++;
        else
        return false;
        if (tab.length == compteur)
        return true
        
        
        System.out.println()
        return compteur
    }
    
    
    public static void main(String[] args)
    {
        int[] tab = {2, 4, 6, 12};
        int val = 12;
        if (verifierMul(L,n))
        System.out.println("Les nombres entiers L sont des multiples de " + val);
        else
        System.out.println("Les nombres entiers L ne sont pas des multiples de" + val);
    }
}
        
