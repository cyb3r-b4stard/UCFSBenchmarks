public class AlphNames{
    public static void main(String Rwargs[])throws IOExecption{
        Scanner tp=new Scanner(new File"C:\\Names.in");
        int maxIndx=-1;
        String mex[]= new String[100];
        while(sf.hasNext()){
            maxIndx++;
            text[maxIndx]=sf.nextLine();
        }
        sf.close();
        String reversedName[]= new String[maxIndx+1];
        for(int j=0;j<=maxIndx;j++){
            Scanner sc=new Scanner(mex[j]);
            String firstName= sc.next();
            String lastName= sc.next;
            rversedName[j]=(lastName+", "+firstName);
        }
        Array.sort(reversedName);
        for(int j=0;j<=maxIndx;j++)
            System.out.println(reversedName[j]);