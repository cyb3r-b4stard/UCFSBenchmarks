

public class HolaMundo
{
   import java.awt. * ;
   import java.awt.event.*;
   import javax.swing.*;
   public static class Saludo extends JFrame {
       
       private JTextfield;
       
       public static void main (String [] args) {
           Saludo marco = new Saludo ();
           marco.setSize(300, 200);
           morco.crearGUI();
           marco.setVisible(true);
        }
        private void createGUI() {
            setDefaultCloseaperation(EXIST_ON_CLOSE)
            Container window = getContenetPane();
            window.setLayout(new FlowLayout() );
            camptext = new JtextField("¡HolaMundo!");
            window.add.(campText);
        }
    }