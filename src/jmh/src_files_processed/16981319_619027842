import javax.swing.*;
import java.awt.*;
import java.awt.event.*;

public class Cajero extends JFrame implements ActionListener{
      
    private JPanel pnlBotones;
    
    /*defino que va ir en el panel pnlDatos, 
     *definiendo primero los JLabel y luego las caja de texto JTextField*/
    /*private JLabel lblBlanco;
    private JLabel lblBlanco1;
    private JLabel lblBlanco2;
    private JLabel lblBlanco3;*/
    private JLabel lblNombre;
    private JLabel lblApellido;
    private JLabel lblNumeroCuenta;
    private JLabel lblValor;
    
    
    private JTextField txtNombre;
    private JTextField txtApellido;
    private JTextField txtNumeroCuenta;
    private JTextField txtValor;
    
    /*defino que va ir en el panel pnlResultado, 
     *definiendo primero  el JTextarea y creando el JScrollPane*/
    private JTextArea textarea1;
    private JScrollPane scrollpane1;
    
    
    /*defino que va ir en el panel pnlBotones,  en este caso se crean 3 botones
     * donde esta el boton para calcular
     * limpiar(deja las cajas de texto en blanco)
     * salir del aplicativo*/
    
    private JButton btnCuentaVacia;
    private JButton btnCuentaSaldo;
    private JButton btnIngresar;
    private JButton btnRetirar;
    private JButton btnSalir;
    
    /*defino en contenedor que va a tener los tres paneles antes creados
     *  pnlDatos
     *  pnlResultados
     *  pnlBotones  */
    private Container contenedor;
    
    
    public void Cajero(){
    
    this.pnlBotones = new JPanel();
    this.pnlBotones.setLayout(new GridLayout(5,1));
    
    
    this.btnCuentaVacia = new JButton("Crear cuenta Vacia");
    this.btnCuentaSaldo = new JButton("Crear una cuenta con saldo");
    this.btnIngresar = new JButton("Depositar dinero en la cuenta");
    this.btnRetirar = new JButton("Retirar dinero en la cuenta");
    this.btnSalir = new JButton("Salir");
    pnlBotones.add(btnCuentaVacia);
    pnlBotones.add(btnCuentaSaldo);
    pnlBotones.add(btnIngresar);
    pnlBotones.add(btnRetirar);
    pnlBotones.add(btnSalir);



        btnCuentaVacia.addActionListener(this);
        btnCuentaSaldo.addActionListener(this);
        btnIngresar.addActionListener(this);
        btnRetirar.addActionListener(this);
        btnSalir.addActionListener(this);
    
    /*
    this.lblNombre          = new JLabel ("Nombre del Empleado");
    this.txtNombre          = new JTextField (20);
    this.lblApellido        = new JLabel("Apellido del Empleado");
    this.txtApellido        = new JTextField (20);
    this.lblNumeroCuenta    = new JLabel("Numero de Cedula");
    this.txtNumeroCuenta    = new JTextField (20);
    this.textarea1 = new JTextArea (18,54);
    this.scrollpane1 = new JScrollPane (textarea1);
    
    pnlDatos.add(lblNombre);
    pnlDatos.add(txtNombre);
    pnlDatos.add(lblApellido);
    pnlDatos.add(txtApellido);
    pnlDatos.add(lblNumeroCuenta);
    pnlDatos.add(txtNumeroCuenta);
    pnlResultado.add(scrollpane1);*/
    
    
    
    this.contenedor = getContentPane();
        contenedor.setLayout(new BorderLayout());
        contenedor.add(pnlBotones,BorderLayout.CENTER);
       
    
        
        setTitle("NOMINA BARRIOS S.A");
        setSize(400,300);
        setVisible(true);
        setLocationRelativeTo(null);
        setResizable(true);
    
    
    
    }
    
    
     
    
    
    public void actionPerformed(ActionEvent evento) {
        if(evento.getSource()==btnCuentaVacia){
             Vacia();

        }else   if(evento.getSource()==btnCuentaSaldo){
        } else if(evento.getSource()==btnIngresar){
            
        }else if(evento.getSource()==btnRetirar){
        
        }else if(evento.getSource()==btnSalir){
        
        }

    }
    
    public void Vacia(){
   
        Cuenta[] nueva = new Cuenta[50];
        boolean salir = false;
        Icon probar; 
        String nombre = "";
        String apellido = "";
        int numerocuenta =0;
        double valor = 0;
       probar = new ImageIcon("/Users/Barrios/Desktop/fondo.jpeg");
       
        nombre = txtNombre.getText();
        apellido = txtApellido.getText();
        numerocuenta = Integer.parseInt(txtNumeroCuenta.getText());
       
        Cuenta nuevaCuenta = new Cuenta(nombre, valor); /* creamos el objeto llamado nuevaCuenta con la 
                    caracteriztica del constructor creado en la clase cuenta */
                    nueva[numerocuenta] = nuevaCuenta; /* la nueva cuenta la guardamos con el
                    id asignado en el objeto iniciado al principio de esta clase*/ 
                    
                    JOptionPane.showMessageDialog(null,"Señor (a) "
                    + nombre + "\nel numero de su cuenta es : " 
                    + numerocuenta + "\nsu saldo inicial es : " + valor,"cajero",JOptionPane.WARNING_MESSAGE,probar);/* mostrasmos en pantalla los 
                    datos solicitados para confirmar la cuenta creada*/

       
        textarea1.append(""+nombre+"");
    
    }