import objectdraw.*;
import java.awt.event.MouseEvent;
import java.awt.event.MouseListener;

/**
 * ##### # ########### ## ##### ####### ####.
 * 
 * @###### (#### ####) 
 * @####### (# ####### ###### ## # ####)
 */
public class Desktop extends FrameWindowController
{
    private Button button;
    
    private Picture trashData;
    private VisibleImage trashView;
    
    private Folder folderGrabbed;
    
    /*
     * The beginning display of canvas
     */
    public void begin() {
        
        button = new Button( 10, 20, canvas );
        
        trashData = new Picture( "trashempty.png" );
        trashView = trashData.createVisibleImage( 250, 200, canvas );
        
    }
    
    /*
     * When mouse is clicked
     */
    public void onMouseClick( Location point ) {
        double x = point.getX();
        double y = point.getY();
        
        if( button.contains(point) ) {
            new Folder( 10, 50, 1, canvas );
        }
        
        if() {
        }
    }
    
    /*
     * When mouse is dragged
     */
}