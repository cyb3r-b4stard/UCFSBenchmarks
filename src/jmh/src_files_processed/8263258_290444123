
import java.awt.FlowLayout;
import java.awt.HeadlessException;

import javax.swing.Icon;
import javax.swing.ImageIcon;
import javax.swing.JFrame;
import javax.swing.JLabel;

public class Welcome extends SlidePuzzle {
  public Welcome() throws HeadlessException {
    setSize(300, 300);
    setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    setLayout(new FlowLayout(FlowLayout.CENTER));
    
    Icon icon = new ImageIcon("slide.jpg");
    JLabel label1 = new JLabel(" WELCOME TO OUR GAME...", icon, JLabel.CENTER);

    JLabel label2 = new JLabel("HAVE FUN!! ", JLabel.CENTER);
    label2.setIcon(new ImageIcon("gudluck.jpg"));
    
    JButton btnEnter = new JButton("ENTER");
    contentPane
    

    getContentPane().add(label1);
    getContentPane().add(label2);
    
  }

  public static void main(String[] args) {
    new Welcome().setVisible(true);
  
     }
}
