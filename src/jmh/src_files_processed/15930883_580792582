import java.util.Scanner;
public class array
{
   public static void main(String [] args){
       Scanner lb=new Scanner(System.in);
        int s,x;
        boolean found = false;
        System.out.println("Number of arrays: ");
        s= lb.nextInt();
       int [] nm= new int[s];
       for(int i=0;i<s;i++){
           System.out.print("Enter the Number:  ");
           
           nm[i]=lb.nextInt();
           
           
        }
        System.out.println("The Array elements are :");
        for(int cnt = 0; cnt < nm.length; cnt++)
System.out.println(nm[cnt]);
System.out.print("Enter the number to cheak : ");
 x=lb.nextInt();
nm[x]=0;

i=0
while(i<nm.length && !found) {
if(myArray[i] == x) 
found=true;
else
i++;
} 
if(found)
System.out.println(n+" is found at index "+i + " in the array"); 
else
System.out.println(n+" not found in the array");

       for(int i=0;i<nm.length;i++){
       if(nm[i]==0)
         continue;
    else
       System.out.println(nm[i]);

}
}
}