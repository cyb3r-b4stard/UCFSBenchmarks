
import java.util.Scanner;
import java.util.ArrayList;
import java.io.*;
import java.io.FileWriter;
import java.io.PrintWriter;
public class Assignment10
{
    private String name;
    private int playerScore;
        
   public void SoccerPlayers(){
       ArrayList <soccerPlayer> soccerPlayers = new ArrayList<soccerPlayer>();
    }
    
   public void Add(String nm , int score){
       name = nm;
       playerScore = score;
       soccerPlayers.add(new soccerPlayer(name, playerScore));
    }
    
 
    
   public void Remove(){
       Scanner input = new Scanner(System.in);
       for(int x = 0 ; x < soccerPlayers.size ; x++){
           System.out.println(x + "\t" + soccerPlayers.get(x));
        }
       System.out.println("Which one do you want to remove?");
       int remove = input.nextInt();
       soccerPlayers.remove(remove);
    }
    
     public String toString(){
        return name + "|" + score; 
    }
    
    public void writeSoccerPlayers(){
        try{
            FileWriter fw = new FileWriter("SoccerPlayers.txt");
            while(true){
                String name;
                int score;
                System.out.println("Please enter the name!");
                name = input.nextLine();
                if (name.equals("exit")){
                    fw.close(); 
                    break;
                }
                System.out.println("Please enter in the score!");
                score = input.nextInt():
                SoccerPlayer sp  = new SoccerPlayer(name, score);
                soccerPlayers.add(sp);
                fw.write(sp.toString());

    }
    
   public void Replace(){
        for(int x = 0 ; x < soccerPlayers.size ; x++){
           System.out.println(x + "\t" + soccerPlayers.get(x));
        }
       System.out.println("Which one do you want to replace?");
       int replace = input.nextInt();
       System.out.println("What's the name");
       String name = input.nextString();
       System.out.println("What's the player's score");
       int score = input.nextInt();
       soccerPlayers.set(replace , new Soccerplayer(name, score));
    }
    
   public void Display(){
        for(int x = 0 ; x < soccerPlayers.size ; x++){
           System.out.println(x + "\t" + soccerPlayers.get(x));
        }
    }
    
  
    }
    
    public void Sort(){
    }
    
    
    
}