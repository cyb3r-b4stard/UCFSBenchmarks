import java.util.*;
import java.io.*;
class Partners {
    public static void main(String[] args) 
    {
        
        
        ArrayList<String> partners = new ArrayList<String>();
              Scanner fileScan, urlScan;
              fileScan = new Scanner (new File("students.txt");
                while (fileScan.hasNext())
                  {
                    int count = 0;
                 urlScan = new Scanner (partners.get(count));
                 urlScan.useDelimiter(",");
                 while (urlScan.hasNext())
                 {
                  partners.add(urlScan.next());
                 }
                  
                 count++;
                }
        
        /*Scanner scan = new Scanner (System.in);
        System.out.println("Enter how many students you want to arrange: ");
                 int count = scan.nextInt();
            
            
                for (int i=0; i < count; i++) 
                {
                    System.out.println("Enter a student name:");
                    String student = scan.next();
                    partners.add(student);
                }
                
                Collections.shuffle(partners);
                int rnd = new Random().nextInt(partners.size());
                int counter = 0;
                System.out.println("Partners: ");
                 for (int i=0; i < (partners.size()/2); i++) 
                    {
                        if (partners.size() % 2 !=0)
                        {
                            System.out.println(partners.get(rnd) + " ,Sorry you will be alone in this project :/ ");
                            partners.remove(rnd);
                        }  
                        
                        System.out.println( (i+1) + ": " + partners.get(counter) + " with " + partners.get(counter + 1) );
                        counter = counter + 2;
                    }
            */
                
    }
}
