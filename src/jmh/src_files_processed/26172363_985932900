
import java.util.Scanner;

 public class P2 {
 public static boolean progEnded = false;
 public static void checkProgStatus(String str) {
 if (str.equals("stop")) {
 System.exit(0); 
 progEnded = true; 
 }
 }
 public static void main(String[] args) {
         System.out.println("Payroll Program Activated");
         Scanner Pay = new Scanner (System.in);
         String hrStr =new String(20);
         String hwStr =new String(20);
          double hr, hw, gp;
          int i = 0;
         for(i=0;i<5;++i)
         {
          System.out.println("Enter Employee Name or type stop too TerminateProgram: ");
          String hrStr[i] = Pay.nextLine();
          checkProgStatus(name); 
         
          System.out.println("Enter Employee Hourly Rate: ");
          
        }
          hrStr = Pay.nextLine(); 
         
          checkProgStatus(hrStr); 
          hr = Double.valueOf(hrStr); 
          while (hr<0 && progEnded != true){
          System.out.println("Re‐enter Previous Field as a Positive Number");
          System.out.println("Enter Employee Hourly Rate: ");
         
          hrStr = Pay.nextLine();
         
          checkProgStatus(hrStr);
          hr = Double.valueOf(hrStr);
          }
          System.out.println("Enter Employee Hours Worked: ");
          hwStr =Pay.nextLine();
         
          checkProgStatus(hwStr);
          hw = Double.valueOf(hwStr);
          while (hw<0 && progEnded != true){
          System.out.println("Re‐enter Previous Field as a Positive Number");
          System.out.println("Enter Employee Hours Worked: ");
          hwStr =Pay.nextLine();
         
          checkProgStatus(hwStr);
          hw = Double.valueOf(hwStr);
          }
          gp = hr * hw;
         System.out.println(name);
         System.out.printf("Employee Hourly Rate:$%.2f\n", + hr);
         System.out.printf("Employee Hours Worked: %.2f\n", + hw);
        System.out.printf("Gross 01/12/2015 Basic Employee Management project in Java| DEVELOPE BY ______ GROUP Pay: $%.2f\n", + gp);
         }
 }
