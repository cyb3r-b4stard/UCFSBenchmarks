import java.util.ArrayList;

public class TileGame
{
   private NumberTile[] myTiles = new NumberTile[2];
   private ArrayList<NumberTile>Gameboard = new ArrayList<NumberTile>();
   NumberTile tile1 = new NumberTile (4,3,7,4);
   NumberTile tile2 = new NumberTile (6,4,3,3);
   NumberTile tile3 = new NumberTile (1,2,3,4);
   NumberTile tile4 = new NumberTile (3,2,5,2);
   NumberTile tile5 = new NumberTile (5,9,2,2);
   public TileGame()
   {
      
      myTiles[0] = new NumberTile(4, 2, 9, 2 );
      myTiles[1] = new NumberTile( 4, 2, 9, 8 );
           
      Gameboard = new ArrayList<NumberTile>();
      Gameboard.add[tile];
      Gameboard.addNumberTile(6,4,3,3);
      Gameboard.addNumberTile(1,2,3,4);
      Gameboard.addNumberTile(3,2,5,2);
      Gameboard.addNumberTile(5,9,2,2);
   } 
       
   public int getIndexForFit( NumberTile tile )
   {
      if (gameboard == null)
      {
          return 0;
      } 
      if (myTiles.getRight() == myTiles.getLeft())
      {
          return 0;
      }    
    }
   public boolean insertTile()
      {
          return false;    
      } 
      
   public String toString()
   {
      String output;
      for (int index = 0; index < ArrayList.length; index++)
      {
          handTiles = myTiles[index];               
      } 
      
      for (int index = 0; index < ArrayList.length; index++)
      {
          gameTiles = gameboard[index]; 
      } 
   }
}
 
   