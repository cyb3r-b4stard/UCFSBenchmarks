
/**
 * ##### # ########### ## ##### ## ####.
 * 
 * @###### (#### ####) 
 * @####### (# ####### ###### ## # ####)
 */
import java.util.*;
import java.util.ArrayList;
import java.text.*;

public class GA
{
    public static void main(String[] args){
    }

    public static List<Item> itemList(List<Item> myItemList) { 
        myItemList = new List<Item>();
        KnapSack ga = new KnapSack(50);
        
        System.out.println("Please Enter Population Size (Number of items on list for GA to choose from): "); 
        Scanner popSizeInput = new Scanner(System.in);
        int popSizeInput = scan.nextInt();

        System.out.println("Please Enter \n1. to add an item to the list or \n2. to finish adding items to the list \n(Each item needs a name, value, and weight in lbs): "); 
        Scanner selection = new Scanner(System.in);
        int selection = scan.nextInt();

        do{
            if (selection =1);

            Scanner name = new Scanner(System.in);
            System.out.print("Enter the name, weight, and value of the item: ");
            Scanner weight = new Scanner (System.in);
            Scanner value = new Scanner (System.in);

            for(int i = 0; i<popSizeInput.size(); i++){
                myItemList.add(name.next(), value.next(), weight.next());
                Item item;(i) = new Item(index.popSizeInput(i));
            } 

        }while (selection = 2);
        
        System.out.println("Please Enter number of maximum generations allowed:"); 
        Scanner maxGenerationInput = new Scanner(System.in);
        int maxGenerationInput = scan.nextInt();

        System.out.println("Please Enter the crossover probability (percentage in decimal form):"); 
        Scanner uniformRate = new Scanner(System.in);
        int uniformRate = scan.nextInt();

        System.out.println("Please Enter the crossover probability (percentage in decimal form):"); 
        Scanner crossoverProbability = new Scanner(System.in);
        int crossoverProbability = scan.nextInt();
        
        List<Item> myItemList = ga.calcSolution();
 
        if (ga.isCalculated()) {
            NumberFormat nf  = NumberFormat.getInstance();
 
            System.out.println(
                "Maximized Sum           = " +
                nf.format(ga.getMaxSum() / 100.0) + " lbs"
            );
            System.out.println(
                "Total weight of solution = " +
                nf.format(ga.getSolutionSum() / 100.0) + " lbs"
            );
            System.out.println(
                "Total value              = " +
                ga.getValue()
            );
            System.out.println();
            System.out.println(
                "You can carry the following materials " +
                "in the knapsack:"
            );
            for (Item item : myItemList) {
                if (item.getInKnapsack() == 1) {
                    System.out.format(
                        "%1$-23s %2$-3s %3$-5s %4$-15s \n",
                        item.getName(),
                        item.getWeight(), "lbs  ",
                        "(value = " + item.getValue() + ")"
                    );
                }
            }
        } else {
            System.out.println(
                "The problem is not solved. " +
                "Maybe you gave wrong data."
            );
        }
 
    }
        
                 

    }
}
