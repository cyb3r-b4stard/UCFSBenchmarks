import java.util.Scanner;
import java.util.ArrayList;
import java.util.Date;
import java.io.*;
public class doorAccessSystem
{
    public static void main(String [] args){
        System.out.println("Welcome to door access system.");
        doorList d = new doorList();
        userList u = new userList();
        d.List();
        u.List();
        boolean validID = false;

        do{
            System.out.println("Please enter a user id or type -1 to exit");
            Scanner keyboard = new Scanner(System.in);
            String userId = keyboard.nextLine();
            for(int i = 0 ; i < u.userList.size() ; i++){
                if( userId.equals(user.id)){
                    validId = true;
                }else{
                    vaildID = false;
                }

            } 
        }while(validID);

        if (user instanceof Administrator ){}
        else{
            if(user instanceof Student ){
                System.out.println(" wwwww");
                ((Student)user).welcome();
            }else if (user instanceof Staff){
                ((Staff)user).welcome();
            }else if (user instanceof Others){
                ((Others)user).welcome();
            }

        }

    }